// ==UserScript==
// @name         华医网学习助手 v6
// @namespace    https://github.com/wzgrx/hua-yi-helper
// @version      8.8.5
// @description  华医网年度 5+20 学分智能规划、断点恢复与跨页面自动学习助手
// @author       wzgrx
// @license      AGPL-3.0
// @match        *://*.91huayi.com/*
// @run-at       document-idle
// @noframes
// @grant        GM_getValue
// @grant        GM_setValue
// @grant        GM_deleteValue
// @grant        GM_registerMenuCommand
// @grant        GM_xmlhttpRequest
// @grant        unsafeWindow
// @connect      127.0.0.1
// @connect      localhost
// @downloadURL  https://raw.githubusercontent.com/wzgrx/hua-yi-helper/main/src/tampermonkey/hua-yi-helper.user.js?v=8.8.5
// @updateURL    https://raw.githubusercontent.com/wzgrx/hua-yi-helper/main/src/tampermonkey/hua-yi-helper.user.js?v=8.8.5
// ==/UserScript==

// Generated by scripts/build-userscript.js. Edit src/shared/core.js or src/tampermonkey/runtime.js.
(function (root, factory) {
  var api = factory();
  if (typeof module === 'object' && module.exports) module.exports = api;
  else root.HuayiCore = api;
})(typeof window !== 'undefined' ? window : (typeof globalThis !== 'undefined' ? globalThis : this), function () {
  'use strict';

  var DEFAULT_POLICY = {
    year: new Date().getFullYear(),
    publicTarget: 5,
    otherTarget: 20
  };

  function clean(value) {
    return String(value == null ? '' : value).replace(/\s+/g, ' ').trim();
  }

  function normalize(value) {
    return clean(value)
      .replace(/^\d+[、.．)\]\s]+/, '')
      .replace(/m²/gi, 'm2')
      .replace(/[，,。；;：:\s]/g, '')
      .toLowerCase();
  }

  function parseCredit(value) {
    var match = clean(value).match(/(\d+(?:\.\d+)?)\s*学?\s*分/);
    return match ? Number(match[1]) : 0;
  }

  function parseDuration(value) {
    var text = clean(value);
    var hours = text.match(/(\d+(?:\.\d+)?)\s*(?:小时|学时|h(?:ours?)?)/i);
    if (hours) return Math.round(Number(hours[1]) * 60);
    var minutes = text.match(/(\d+(?:\.\d+)?)\s*(?:分钟|min(?:utes?)?)/i);
    if (minutes) return Math.round(Number(minutes[1]));
    var clock = text.match(/(\d{1,2}):(\d{2})(?::(\d{2}))?/);
    if (!clock) return 0;
    return clock[3] ? Number(clock[1]) * 60 + Number(clock[2]) + Math.ceil(Number(clock[3]) / 60) :
      Number(clock[1]) * 60 + Number(clock[2]);
  }

  function detectCategory(value) {
    var text = clean(value);
    if (/公需|公共必修|公共科目/.test(text)) return 'public';
    if (/全员|专项/.test(text)) return 'other';
    if (/继续教育|继教|国家级|省级|市级|自治区级|专业科目|专业课/.test(text)) return 'other';
    return 'other';
  }

  function detectSource(value) {
    var text = clean(value);
    if (/公需|公共必修|公共科目/.test(text)) return '继续教育公需课';
    if (/全员|专项/.test(text)) return '全员专项';
    if (/继续教育|继教|国家级|省级|市级|自治区级/.test(text)) return '继续教育';
    return '其他';
  }

  function detectStatus(value) {
    var text = clean(value);
    if (/已申请|已获学分|已授予/.test(text)) return 'applied';
    if (/申请证书|申请学分|待申请|学习完毕|本项目已完成/.test(text)) return 'completed';
    if (/待考试/.test(text)) return 'exam';
    if (/学习中|继续学习|播放至|进行中/.test(text)) return 'in_progress';
    if (/未学习|开始学习|立即学习/.test(text)) return 'not_started';
    if (/已完成/.test(text)) return 'completed';
    return 'unknown';
  }

  function parseYear(value, fallback) {
    var years = clean(value).match(/20\d{2}/g);
    return years && years.length ? Number(years[0]) : Number(fallback || DEFAULT_POLICY.year);
  }

  function normalizeRecord(record, options) {
    options = options || {};
    var text = clean(record.text || [
      record.name, record.creditText, record.categoryText, record.sourceText,
      record.statusText, record.progressText, record.durationText
    ].join(' '));
    var status = record.status || detectStatus(record.statusText || text);
    var category = record.category || detectCategory(record.categoryText || text);
    return {
      id: clean(record.id || record.cwid || record.cid || record.url || record.name),
      name: clean(record.name || ''),
      url: clean(record.url || ''),
      year: Number(record.year || parseYear(text, options.year)),
      credit: Number(record.credit || parseCredit(record.creditText || text)),
      durationMinutes: Number(record.durationMinutes || parseDuration(record.durationText || text)),
      category: category,
      source: record.source || detectSource(record.sourceText || text),
      status: status,
      enrolled: record.enrolled !== false,
      action: clean(record.action || ''),
      progress: clean(record.progress || record.progressText || ''),
      text: text
    };
  }

  function policyWithDefaults(policy) {
    return Object.assign({}, DEFAULT_POLICY, policy || {});
  }

  function summarizeCredits(records, policy) {
    policy = policyWithDefaults(policy);
    var normalized = (records || []).map(function (record) {
      return normalizeRecord(record, policy);
    }).filter(function (record) {
      return !record.year || record.year === Number(policy.year);
    });
    var summary = {
      year: Number(policy.year),
      publicTarget: Number(policy.publicTarget),
      otherTarget: Number(policy.otherTarget),
      publicEarned: 0,
      publicCommitted: 0,
      otherEarned: 0,
      otherCommitted: 0,
      records: normalized,
      applicationTasks: [],
      resumeTasks: []
    };
    normalized.forEach(function (record) {
      var prefix = record.category === 'public' ? 'public' : 'other';
      if (record.status === 'applied') summary[prefix + 'Earned'] += record.credit;
      else if (record.enrolled && /^(completed|exam|in_progress|not_started)$/.test(record.status)) {
        summary[prefix + 'Committed'] += record.credit;
      }
      if (record.status === 'completed' && record.url) summary.applicationTasks.push(record);
      else if (record.enrolled && /^(exam|in_progress|not_started)$/.test(record.status) && record.url) {
        summary.resumeTasks.push(record);
      }
    });
    summary.publicProjected = summary.publicEarned + summary.publicCommitted;
    summary.otherProjected = summary.otherEarned + summary.otherCommitted;
    summary.publicDeficit = Math.max(0, summary.publicTarget - summary.publicProjected);
    summary.otherDeficit = Math.max(0, summary.otherTarget - summary.otherProjected);
    summary.totalEarned = summary.publicEarned + summary.otherEarned;
    summary.totalProjected = summary.publicProjected + summary.otherProjected;
    summary.satisfied = summary.publicEarned >= summary.publicTarget && summary.otherEarned >= summary.otherTarget;
    summary.projectedSatisfied = summary.publicProjected >= summary.publicTarget &&
      summary.otherProjected >= summary.otherTarget;
    return summary;
  }

  function candidateCost(path, target) {
    var credit = path.reduce(function (sum, item) { return sum + item.credit; }, 0);
    var duration = path.reduce(function (sum, item) {
      return sum + (item.durationMinutes > 0 ? item.durationMinutes : 10000);
    }, 0);
    return {
      overshoot: Math.max(0, credit - target),
      duration: duration,
      count: path.length,
      credit: credit
    };
  }

  function comparePlans(a, b, target) {
    if (!a) return 1;
    if (!b) return -1;
    var ca = candidateCost(a, target);
    var cb = candidateCost(b, target);
    if (ca.overshoot !== cb.overshoot) return ca.overshoot - cb.overshoot;
    if (ca.duration !== cb.duration) return ca.duration - cb.duration;
    if (ca.count !== cb.count) return ca.count - cb.count;
    return ca.credit - cb.credit;
  }

  function chooseOptimalSubset(candidates, target) {
    target = Math.max(0, Number(target || 0));
    if (target <= 0) return { selected: [], credit: 0, shortage: 0 };
    var normalized = (candidates || []).map(function (candidate) {
      return normalizeRecord(Object.assign({}, candidate, { enrolled: false }));
    }).filter(function (candidate) {
      return candidate.credit > 0 && candidate.url;
    });
    var scale = 10;
    var targetUnits = Math.ceil(target * scale - 1e-9);
    var maxUnits = targetUnits + normalized.reduce(function (max, item) {
      return Math.max(max, Math.round(item.credit * scale));
    }, 0);
    var plans = new Map();
    plans.set(0, []);
    normalized.slice(0, 80).forEach(function (item) {
      var units = Math.round(item.credit * scale);
      Array.from(plans.entries()).sort(function (a, b) { return b[0] - a[0]; }).forEach(function (entry) {
        var nextUnits = entry[0] + units;
        if (nextUnits > maxUnits) return;
        var nextPlan = entry[1].concat(item);
        var existing = plans.get(nextUnits);
        if (!existing || comparePlans(nextPlan, existing, target) < 0) plans.set(nextUnits, nextPlan);
      });
    });
    var best = null;
    plans.forEach(function (plan, units) {
      if (units < targetUnits) return;
      if (!best || comparePlans(plan, best, target) < 0) best = plan;
    });
    if (!best) {
      var allCredit = normalized.reduce(function (sum, item) { return sum + item.credit; }, 0);
      return { selected: normalized, credit: allCredit, shortage: Math.max(0, target - allCredit) };
    }
    var credit = best.reduce(function (sum, item) { return sum + item.credit; }, 0);
    return { selected: best, credit: credit, shortage: Math.max(0, target - credit) };
  }

  function uniqueById(records) {
    var seen = {};
    return records.filter(function (record) {
      var key = record.id || record.url || normalize(record.name);
      if (!key || seen[key]) return false;
      seen[key] = true;
      return true;
    });
  }

  function buildAnnualPlan(records, catalog, policy) {
    policy = policyWithDefaults(policy);
    var summary = summarizeCredits(records, policy);
    var completed = summary.records.filter(function (record) {
      return record.status === 'completed';
    });
    var resumable = summary.records.filter(function (record) {
      return record.enrolled && /^(exam|in_progress|not_started)$/.test(record.status) && record.url;
    });
    var completedPublic = completed.filter(function (record) { return record.category === 'public'; })
      .reduce(function (sum, record) { return sum + record.credit; }, 0);
    var completedOther = completed.filter(function (record) { return record.category !== 'public'; })
      .reduce(function (sum, record) { return sum + record.credit; }, 0);
    var publicResume = chooseOptimalSubset(resumable.filter(function (record) {
      return record.category === 'public';
    }), Math.max(0, summary.publicTarget - summary.publicEarned - completedPublic));
    var otherResume = chooseOptimalSubset(resumable.filter(function (record) {
      return record.category !== 'public';
    }), Math.max(0, summary.otherTarget - summary.otherEarned - completedOther));
    var publicAfterExisting = summary.publicEarned + completedPublic + publicResume.credit;
    var otherAfterExisting = summary.otherEarned + completedOther + otherResume.credit;
    var enrolledIds = {};
    summary.records.forEach(function (record) {
      enrolledIds[record.id || record.url || record.name] = true;
    });
    var available = uniqueById((catalog || []).map(function (record) {
      return normalizeRecord(Object.assign({}, record, { enrolled: false }), policy);
    }).filter(function (record) {
      var key = record.id || record.url || record.name;
      return record.year === Number(policy.year) && record.credit > 0 && record.url &&
        !enrolledIds[key] && !/^(applied|completed)$/.test(record.status);
    }));
    var publicChoice = chooseOptimalSubset(available.filter(function (item) {
      return item.category === 'public';
    }), Math.max(0, summary.publicTarget - publicAfterExisting));
    var selectedPublicIds = {};
    publicChoice.selected.forEach(function (item) { selectedPublicIds[item.id || item.url] = true; });
    var otherChoice = chooseOptimalSubset(available.filter(function (item) {
      return item.category !== 'public' && !selectedPublicIds[item.id || item.url];
    }), Math.max(0, summary.otherTarget - otherAfterExisting));
    var tasks = [];
    summary.applicationTasks.forEach(function (record) {
      tasks.push({ type: 'apply', priority: 10, record: record, reason: '课程已完成，先申请学分' });
    });
    publicResume.selected.concat(otherResume.selected).sort(function (a, b) {
      if (a.category !== b.category) return a.category === 'public' ? -1 : 1;
      return b.credit - a.credit;
    }).forEach(function (record) {
      tasks.push({ type: 'resume', priority: record.category === 'public' ? 20 : 30, record: record, reason: '继续已投入课程' });
    });
    publicChoice.selected.forEach(function (record) {
      tasks.push({ type: 'enroll', priority: 40, record: record, reason: '补足公需课 5 分目标' });
    });
    otherChoice.selected.forEach(function (record) {
      tasks.push({ type: 'enroll', priority: 50, record: record, reason: '补足其他课程 20 分目标' });
    });
    tasks.sort(function (a, b) { return a.priority - b.priority; });
    return {
      policy: policy,
      summary: summary,
      publicResumeSelection: publicResume,
      otherResumeSelection: otherResume,
      publicSelection: publicChoice,
      otherSelection: otherChoice,
      tasks: tasks,
      projectedPublic: publicAfterExisting + publicChoice.credit,
      projectedOther: otherAfterExisting + otherChoice.credit,
      shortages: {
        public: publicChoice.shortage,
        other: otherChoice.shortage
      },
      satisfied: publicChoice.shortage <= 0 && otherChoice.shortage <= 0
    };
  }

  return {
    VERSION: '8.1.1',
    DEFAULT_POLICY: DEFAULT_POLICY,
    clean: clean,
    normalize: normalize,
    parseCredit: parseCredit,
    parseDuration: parseDuration,
    parseYear: parseYear,
    detectCategory: detectCategory,
    detectSource: detectSource,
    detectStatus: detectStatus,
    normalizeRecord: normalizeRecord,
    summarizeCredits: summarizeCredits,
    chooseOptimalSubset: chooseOptimalSubset,
    buildAnnualPlan: buildAnnualPlan
  };
});

(function () {
  'use strict';

  var Core = window.HuayiCore;
  if (!Core) throw new Error('HuayiCore 未加载');
  var VERSION = '8.8.5';
  var STATE_KEY = 'HY8_STATE';
  var ANSWER_KEY = 'HY8_ANSWERS';
  var ANSWER_OPTION_KEY = 'HY8_ANSWER_OPTIONS';
  var EXAM_KEY = 'HY8_EXAMS';
  var CLASSROOM_KEY = 'HY8_CLASSROOM';
  var POLICY_KEY = 'HY8_POLICY';
  var LOGIN_KEY = 'HY8_LOGIN';
  var CAPTCHA_CONFIG_KEY = 'HY8_CAPTCHA_CONFIG';
  var CARD_RETRY_MS = 30 * 60 * 1000;
  var pageWindow = typeof unsafeWindow !== 'undefined' ? unsafeWindow : window;

  function read(key, fallback) {
    try { var value = GM_getValue(key); return value === undefined || value === null ? fallback : value; }
    catch (_) { return fallback; }
  }
  function write(key, value) { try { GM_setValue(key, value); } catch (_) {} }
  function remove(key) { try { GM_deleteValue(key); } catch (_) {} }
  function now() { return new Date().toLocaleTimeString('zh-CN', { hour12: false }); }
  var clean = Core.clean;
  var normalize = Core.normalize;
  function migrateLegacy() {
    if (read(STATE_KEY, null) == null) write(STATE_KEY, read('HY7_STATE', {}));
    if (read(ANSWER_KEY, null) == null) write(ANSWER_KEY, read('HY7_ANSWERS', {}));
    if (read(ANSWER_OPTION_KEY, null) == null) write(ANSWER_OPTION_KEY, {});
    if (read(EXAM_KEY, null) == null) write(EXAM_KEY, read('HY7_EXAMS', {}));
  }
  migrateLegacy();
  var policy = Object.assign({
    year: new Date().getFullYear(),
    publicTarget: 5,
    otherTarget: 20
  }, read(POLICY_KEY, {}));
  CARD_RETRY_MS = Math.max(60 * 1000, Number(policy.cardRetryMinutes || 30) * 60 * 1000);
  var TARGET = Number(policy.publicTarget) + Number(policy.otherTarget);
  function registerMenus() {
    if (typeof GM_registerMenuCommand !== 'function') return;
    GM_registerMenuCommand('设置年度学分目标', function () {
      var year = Number(prompt('目标年度', String(policy.year)) || policy.year);
      var publicTarget = Number(prompt('公需课目标学分', String(policy.publicTarget)) || policy.publicTarget);
      var otherTarget = Number(prompt('其他课程目标学分', String(policy.otherTarget)) || policy.otherTarget);
      if (!year || publicTarget < 0 || otherTarget < 0) return;
      policy = Object.assign({}, policy, {
        year: year,
        publicTarget: publicTarget,
        otherTarget: otherTarget
      });
      CARD_RETRY_MS = Math.max(60 * 1000, Number(policy.cardRetryMinutes || 30) * 60 * 1000);
      TARGET = publicTarget + otherTarget;
      write(POLICY_KEY, policy);
      setState({ message: '年度目标已更新，请重新扫描学习记录' });
    });
    GM_registerMenuCommand('设置本机自动登录', function () {
      var current = read(LOGIN_KEY, {});
      var username = prompt('用户名/身份证号（仅保存于本机脚本存储）', current.username || '');
      if (username === null) return;
      var password = prompt('密码（仅保存于本机脚本存储）', current.password || '');
      if (password === null) return;
      write(LOGIN_KEY, { username: clean(username), password: String(password) });
      setState({ message: '本机登录信息已保存' });
    });
    GM_registerMenuCommand('清除本机登录信息', function () {
      remove(LOGIN_KEY);
      setState({ message: '本机登录信息已清除' });
    });
    GM_registerMenuCommand('设置本机验证码识别服务', function () {
      var current = read(CAPTCHA_CONFIG_KEY, {});
      var provider = prompt(
        '本机验证码识别服务地址',
        current.providerUrl || 'http://127.0.0.1:17891/solve'
      );
      if (provider === null) return;
      write(CAPTCHA_CONFIG_KEY, {
        providerUrl: clean(provider),
        expectedLength: Number(current.expectedLength || 5),
        maxAttempts: Number(current.maxAttempts || 6)
      });
      setState({ message: '本机验证码识别服务已更新' });
    });
  }
  function absolute(url) {
    try { return new URL(url, location.href).href; } catch (_) { return url || ''; }
  }
  function queryParam(name, url) {
    try { return new URL(url || location.href).searchParams.get(name) || ''; } catch (_) { return ''; }
  }
  function navigate(url) {
    if (!url) return;
    var target = absolute(url);
    if (target === location.href) return;
    location.assign(target);
  }
  function visible(element) {
    if (!element) return false;
    var current = element;
    while (current && current.nodeType === 1) {
      var style = getComputedStyle(current);
      if (style.display === 'none' || style.visibility === 'hidden' || style.opacity === '0') return false;
      current = current.parentElement;
    }
    return true;
  }
  function enabled(element) {
    if (!element || !visible(element)) return false;
    return !element.disabled && !element.hasAttribute('disabled') &&
      clean(element.getAttribute('aria-disabled')).toLowerCase() !== 'true' &&
      !/(^|\s)(disabled|is-disabled|inputstyle2_2)(\s|$)/i.test(String(element.className || ''));
  }
  function pageText() {
    return document.body ? document.body.innerText || document.body.textContent || '' : '';
  }
  function classText(element) {
    return String(element && element.className || '');
  }
  function elementRect(element) {
    try {
      var rect = element.getBoundingClientRect();
      return { x: Math.round(rect.x), y: Math.round(rect.y), w: Math.round(rect.width), h: Math.round(rect.height) };
    } catch (_) {
      return { x: 0, y: 0, w: 0, h: 0 };
    }
  }
  function looksClickable(element) {
    if (!element) return false;
    var tag = String(element.tagName || '').toLowerCase();
    if (/^(button|a|input|select|textarea)$/.test(tag)) return true;
    if (element.getAttribute('role') === 'button') return true;
    if (element.hasAttribute('onclick') || element.hasAttribute('tabindex')) return true;
    if (/(btn|button|click|view|next|submit|primary|footer|case|start|continue)/i.test(classText(element))) return true;
    return false;
  }
  function uniqueElements(list) {
    var seen = [];
    return list.filter(function (item) {
      if (!item || seen.indexOf(item) >= 0) return false;
      seen.push(item);
      return true;
    });
  }
  function findSemanticActions(pattern, denyPattern, options) {
    options = options || {};
    var selectors = options.selectors || [
      'button', 'a[href]', 'input[type="button"]', 'input[type="submit"]',
      '[role="button"]', '[onclick]', '[tabindex]',
      '.van-button', '.nut-button', '.adm-button', '.ant-btn',
      'span[class]', 'div[class]'
    ].join(',');
    return uniqueElements(Array.from(document.querySelectorAll(selectors))).map(function (element) {
      var text = actionText(element);
      var patternMatched = !pattern || pattern.test(text);
      var rect = elementRect(element);
      var score = 0;
      if (patternMatched && pattern) score += 100;
      if (looksClickable(element)) score += 15;
      if (rect.w > 20 && rect.h > 14) score += 8;
      if (denyPattern && denyPattern.test(text)) score -= 200;
      if (!enabled(element)) score -= 200;
      if (!text || text.length > (options.maxText || 22)) score -= 30;
      if (!patternMatched && !options.loose) score -= 200;
      if (options.score) score += options.score(element, text, rect) || 0;
      return { element: element, text: text, score: score, rect: rect };
    }).filter(function (item) {
      return item.score > 0;
    }).sort(function (a, b) { return b.score - a.score; });
  }
  function parseClock(value) {
    var parts = String(value || '').split(':').map(function (part) { return Number(part); });
    if (parts.some(function (part) { return !isFinite(part); })) return 0;
    if (parts.length === 2) return parts[0] * 60 + parts[1];
    if (parts.length === 3) return parts[0] * 3600 + parts[1] * 60 + parts[2];
    return 0;
  }
  function caseVideoStatus() {
    var media = Array.from(document.querySelectorAll('video')).find(function (video) {
      return visible(video) && isFinite(Number(video.duration)) && Number(video.duration) > 0;
    });
    if (media) {
      var mediaCurrent = Math.max(0, Number(media.currentTime || 0));
      var mediaDuration = Math.max(0, Number(media.duration || 0));
      var mediaRemaining = Math.max(0, mediaDuration - mediaCurrent);
      var mediaKey = location.href;
      var finishedNow = !!media.ended || (mediaDuration > 0 && mediaCurrent >= mediaDuration - 0.25);
      var completed = window.__HY8_CASE_VIDEO_DONE;
      if (finishedNow) {
        completed = { key: mediaKey, at: Date.now() };
        window.__HY8_CASE_VIDEO_DONE = completed;
      }
      var recentlyCompleted = !!(completed && completed.key === mediaKey &&
        Date.now() - Number(completed.at || 0) < 120000);
      return {
        active: mediaDuration > 0,
        done: finishedNow || recentlyCompleted,
        current: mediaCurrent,
        duration: mediaDuration,
        remaining: mediaRemaining,
        paused: !!media.paused,
        media: media
      };
    }
    var text = pageText();
    var match = text.match(/(\d{1,2}:\d{2}(?::\d{2})?)\s*\/\s*(\d{1,2}:\d{2}(?::\d{2})?)/);
    if (!match) return { active: false, done: false, current: 0, duration: 0, remaining: 0, paused: false, media: null };
    var current = parseClock(match[1]);
    var duration = parseClock(match[2]);
    var remaining = Math.max(0, duration - current);
    return {
      active: duration > 0,
      done: duration > 0 && current >= duration - 0.25,
      current: current,
      duration: duration,
      remaining: remaining,
      paused: false,
      media: null
    };
  }
  function actionText(element) {
    if (!element) return '';
    return clean(element.value || element.innerText || element.textContent ||
      element.getAttribute('aria-label') || element.getAttribute('title'));
  }
  function humanClick(element) {
    if (!element || !enabled(element)) return false;
    try {
      ['pointerdown', 'mousedown', 'pointerup', 'mouseup', 'click'].forEach(function (type) {
        element.dispatchEvent(new MouseEvent(type, { bubbles: true, cancelable: true, view: window }));
      });
      return true;
    } catch (_) {
      try { element.click(); return true; } catch (__) { return false; }
    }
  }

  function defaultState() {
    return {
      running: !!read('HY_Running', false), paused: false, phase: 'idle',
      message: '就绪', credit: 0, publicEarned: 0, otherEarned: 0,
      publicProjected: 0, otherProjected: 0, currentCourseUrl: '', currentCourseName: '',
      currentCwid: '', lastRoute: '', lastActionAt: 0,
      blockedApplications: [], blockedApplicationYear: policy.year, blockedApplicationRetryAt: 0,
      cardRetryQueue: [],
      catalogYear: policy.year, catalogVisited: [], catalogSourcesVisited: [],
      studyRecords: [], catalogRecords: [], planTasks: [], logs: []
    };
  }
  var state = Object.assign(defaultState(), read(STATE_KEY, {}));
  function saveState() { state.lastActionAt = Date.now(); write(STATE_KEY, state); }
  function setState(patch) { Object.assign(state, patch || {}); saveState(); render(); }
  function log(message) {
    var line = now() + ' ' + message;
    state.logs = (state.logs || []).concat(line).slice(-40);
    saveState();
    try { console.log('[HY8] ' + line); } catch (_) {}
    render();
  }

  function route() {
    var host = location.hostname.toLowerCase();
    var path = location.pathname.toLowerCase();
    if (host.indexOf('dcwj.') === 0) return 'survey';
    if (host.indexOf('hdbl.') === 0) return 'case';
    if (/\/secure\/login/.test(path)) return 'login';
    if (/study_info_list\.aspx$/.test(path)) return 'study';
    if (/apply_certificate(?:_top)?\.aspx$/.test(path)) return 'certificate';
    if (/card_select\.aspx$/.test(path)) return 'card';
    if (/exam_result(?:_hd)?\.aspx$/.test(path)) return 'result';
    if (/exam_code\.aspx$/.test(path)) return 'captcha';
    if (/exam\.aspx$/.test(path)) return 'exam';
    if (/\/course_ware\/course_ware(?:_(?:polyv|cc))?\.aspx$/.test(path)) return 'player';
    if (/\/course_ware\/course_list\.aspx$/.test(path)) return 'course';
    if (/\/pages\/course\.aspx$/.test(path) && queryParam('cid')) return 'course';
    if (/\/cme\/(?:index|fme)/.test(path) || /\/pages\/(?:cme|fme)\.aspx$/.test(path)) return 'catalog';
    return 'other';
  }

  var hostNode = null;
  var shadow = null;
  function cleanupLegacyPanels() {
    Array.from(document.querySelectorAll('#HY7_HOST,#HY8_HOST')).forEach(function (node) {
      try { node.remove(); } catch (_) {}
    });
  }
  function createUI() {
    if (route() === 'player' || hostNode || !document.body) return;
    cleanupLegacyPanels();
    hostNode = document.createElement('div');
    hostNode.id = 'HY8_HOST';
    hostNode.style.cssText = 'all:initial;position:fixed;top:14px;right:14px;z-index:2147483647;';
    shadow = hostNode.attachShadow({ mode: 'open' });
    shadow.innerHTML = '<style>' +
      ':host{all:initial}.box{width:280px;max-width:calc(100vw - 28px);font:13px/1.45 "Microsoft YaHei",sans-serif;color:#e8eef7;background:#172033;border:1px solid #34445f;border-radius:12px;box-shadow:0 12px 36px #0007;overflow:hidden}' +
      '.head{display:flex;align-items:center;justify-content:space-between;padding:10px 12px;background:#1f5f99;font-weight:700}.head button{width:28px}.body{padding:10px}.row{display:flex;gap:7px;align-items:center;margin:6px 0}.grow{flex:1}.badge{padding:2px 7px;border-radius:999px;background:#263650;color:#9fd1ff}.meter{height:7px;border-radius:9px;background:#263650;overflow:hidden}.bar{height:100%;background:#29b67a}.msg{min-height:36px;color:#dbe7f7}.plan{max-height:126px;overflow:auto;margin-top:7px;padding:7px;background:#101a2a;border-radius:7px;color:#c8d8ee;font-size:11px;white-space:pre-wrap}.plan:empty{display:none}.actions{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin-top:9px}button{box-sizing:border-box;border:0;border-radius:7px;padding:8px;background:#2b6da8;color:white;font:inherit;cursor:pointer}button.pause{background:#a64040}button.secondary{background:#40516c}.log{display:none;max-height:180px;overflow:auto;margin-top:8px;padding:7px;background:#0e1522;color:#8ee79f;border-radius:7px;font:11px/1.45 Consolas,monospace;white-space:pre-wrap}.collapsed .body{display:none}' +
      '</style><section class="box"><header class="head"><span id="title">华医助手 v' + VERSION + '</span><button id="fold" type="button">−</button></header>' +
      '<div class="body"><div class="row"><span id="status" class="badge">就绪</span><span class="grow"></span><strong id="credit">0/' + TARGET + '分</strong></div>' +
      '<div class="row"><span id="publicCredit">公需 0/' + policy.publicTarget + '</span><span class="grow"></span><span id="otherCredit">其他 0/' + policy.otherTarget + '</span></div>' +
      '<div class="meter"><div id="bar" class="bar" style="width:0%"></div></div><div id="message" class="msg"></div><div id="plan" class="plan"></div>' +
      '<div class="actions"><button id="start" type="button">开始/继续</button><button id="pause" class="pause" type="button">暂停</button><button id="records" class="secondary" type="button">学习记录</button><button id="showlog" class="secondary" type="button">日志</button></div>' +
      '<div id="log" class="log"></div></div></section>';
    document.body.appendChild(hostNode);
    shadow.getElementById('fold').addEventListener('click', function (event) {
      event.preventDefault(); event.stopPropagation();
      var box = shadow.querySelector('.box'); box.classList.toggle('collapsed');
      this.textContent = box.classList.contains('collapsed') ? '+' : '−';
    });
    shadow.getElementById('start').addEventListener('click', function (event) {
      event.preventDefault(); event.stopPropagation();
      setState({
        running: true,
        paused: false,
        blockedApplications: [],
        blockedApplicationYear: Number(policy.year),
        blockedApplicationRetryAt: 0,
        cardRetryQueue: [],
        message: '正在恢复流程'
      });
      runRoute(true);
    });
    shadow.getElementById('pause').addEventListener('click', function (event) {
      event.preventDefault(); event.stopPropagation();
      setState({ running: false, paused: true, phase: 'paused', message: '已暂停' });
      if (window.__HY8_TIMER) clearInterval(window.__HY8_TIMER);
    });
    shadow.getElementById('records').addEventListener('click', function (event) {
      event.preventDefault(); event.stopPropagation(); navigate('/pages/study_info_list.aspx');
    });
    shadow.getElementById('showlog').addEventListener('click', function (event) {
      event.preventDefault(); event.stopPropagation();
      var area = shadow.getElementById('log'); area.style.display = area.style.display === 'block' ? 'none' : 'block';
    });
    render();
  }
  function render() {
    if (!shadow) return;
    var status = state.paused ? '已暂停' : (state.running ? '运行中' : '就绪');
    shadow.getElementById('status').textContent = status;
    shadow.getElementById('credit').textContent = Number(state.credit || 0) + '/' + TARGET + '分';
    shadow.getElementById('bar').style.width = Math.min(100, Number(state.credit || 0) / TARGET * 100) + '%';
    shadow.getElementById('publicCredit').textContent = '公需 ' + Number(state.publicEarned || 0) + '/' + policy.publicTarget +
      '（计划 ' + Number(state.publicProjected || 0) + '）';
    shadow.getElementById('otherCredit').textContent = '其他 ' + Number(state.otherEarned || 0) + '/' + policy.otherTarget +
      '（计划 ' + Number(state.otherProjected || 0) + '）';
    shadow.getElementById('message').textContent = state.message || '';
    var taskLabels = { apply: '申请', resume: '继续', enroll: '选课' };
    shadow.getElementById('plan').textContent = (state.planTasks || []).slice(0, 12).map(function (task, index) {
      return (index + 1) + '. [' + (taskLabels[task.type] || task.type) + '] ' + task.name +
        '｜' + task.credit + '分｜' + task.source;
    }).join('\n');
    shadow.getElementById('log').textContent = (state.logs || []).join('\n');
  }

  function extractActionUrl(element) {
    if (!element) return '';
    if (element.href && !/^javascript:/i.test(element.href)) return element.href;
    var raw = element.getAttribute('data-href') || element.getAttribute('data-url') ||
      element.getAttribute('onclick') || '';
    if (/^(?:\/|https?:)/i.test(raw)) return absolute(raw);
    var match = raw.match(/(?:window\.open|location(?:\.href)?\s*=|openCourse|toCourse)\s*\(?\s*['"]([^'"]+)/i);
    if (match) return absolute(match[1]);
    match = raw.match(/((?:\/?course_ware\/)?course_ware[^'"\s]*\.aspx\?[^'"\s]*)/i);
    return match ? absolute(match[1]) : '';
  }

  function rowAction(row) {
    var elements = Array.from(row.querySelectorAll('a,button,input[type="button"],input[type="submit"],[data-href],[onclick]'));
    return elements.find(function (element) {
      return /申请证书|申请学分|继续学习|开始学习|立即学习|进入学习|待考试/.test(actionText(element));
    }) || elements.find(function (element) { return extractActionUrl(element); }) || null;
  }

  function selectStudyYear() {
    var selects = Array.from(document.querySelectorAll('select'));
    var yearSelect = selects.find(function (select) {
      return Array.from(select.options || []).some(function (option) {
        return clean(option.value || option.textContent) === String(policy.year);
      });
    });
    if (!yearSelect || clean(yearSelect.value) === String(policy.year)) return false;
    yearSelect.value = String(policy.year);
    yearSelect.dispatchEvent(new Event('input', { bubbles: true }));
    yearSelect.dispatchEvent(new Event('change', { bubbles: true }));
    return true;
  }

  function scanStudy() {
    var rows = uniqueElements(Array.from(document.querySelectorAll(
      'table tr,.study-item,.study-list-item,[class*="study"][class*="item"],[class*="project"][class*="item"]'
    )));
    var result = { credit: 0, courses: [], records: [], localTasks: [], ready: false, summary: null };
    var sawDataRow = false;
    rows.forEach(function (row) {
      var cells = Array.from(row.querySelectorAll('td'));
      var text = clean(row.innerText || row.textContent);
      if (!text || (!cells.length && !/\d+(?:\.\d+)?\s*学?\s*分/.test(text))) return;
      if (cells.length && cells.length < 3) return;
      if (!/\d+(?:\.\d+)?\s*学?\s*分/.test(text) && !/已申请|学习中|学习完毕|未学习|申请学分|继续学习/.test(text)) return;
      sawDataRow = true;
      var year = Core.parseYear(text, policy.year);
      if (year !== Number(policy.year)) return;
      result.ready = true;
      var link = row.querySelector('a[href*="course.aspx"],a[href*="cid="],a[href*="cwid="]');
      var actionElement = rowAction(row);
      var url = extractActionUrl(actionElement) || (link ? absolute(link.href || link.getAttribute('href')) : '');
      var nameNode = row.querySelector('.course-name,.project-name,[class*="title"],a[href*="course.aspx"]');
      var name = clean(nameNode ? nameNode.textContent : (cells[0] ? cells[0].textContent : ''));
      var creditCell = cells.find(function (cell) {
        return /(?:学分|\d\s*分)/.test(clean(cell.textContent));
      });
      var record = Core.normalizeRecord({
        id: queryParam('cid', url) || queryParam('cwid', url) || name,
        name: name,
        url: url,
        year: year,
        creditText: creditCell ? clean(creditCell.textContent) : text,
        categoryText: text,
        sourceText: text,
        statusText: text,
        action: actionText(actionElement),
        progressText: text,
        enrolled: true,
        text: text
      }, policy);
      result.records.push(record);
      if (actionElement) result.localTasks.push({ record: record, element: actionElement });
    });
    if (!sawDataRow && document.querySelector('table') && /暂无|没有.*学习记录|无学习记录/.test(pageText())) {
      result.ready = true;
    }
    result.summary = Core.summarizeCredits(result.records, policy);
    result.credit = result.summary.totalEarned;
    result.courses = result.records;
    return result;
  }

  function simplifyTasks(tasks) {
    return (tasks || []).map(function (task) {
      return {
        type: task.type,
        reason: task.reason,
        name: task.record.name,
        url: task.record.url,
        credit: task.record.credit,
        category: task.record.category,
        source: task.record.source
      };
    });
  }

  function blockedApplicationDecision(tasks, retryAt, timestamp, queuedUrls) {
    var list = tasks || [];
    if (!list.length) return null;
    var current = Number(timestamp || Date.now());
    var nextRetryAt = Number(retryAt || 0);
    var byUrl = {};
    list.forEach(function (task) {
      if (task && task.record && task.record.url) byUrl[task.record.url] = task;
    });
    var queue = (Array.isArray(queuedUrls) ? queuedUrls : []).filter(function (url, index, urls) {
      return byUrl[url] && urls.indexOf(url) === index;
    });
    if (queue.length) {
      var queuedUrl = queue.shift();
      return {
        action: 'retry',
        task: byUrl[queuedUrl],
        queue: queue,
        retryAt: nextRetryAt || current + CARD_RETRY_MS,
        waitMs: 0
      };
    }
    if (!nextRetryAt || current >= nextRetryAt) {
      queue = list.slice(1).map(function (task) { return task.record.url; });
      return {
        action: 'retry',
        task: list[0],
        queue: queue,
        retryAt: current + CARD_RETRY_MS,
        waitMs: 0
      };
    }
    return {
      action: 'wait',
      task: null,
      queue: [],
      retryAt: nextRetryAt,
      waitMs: Math.max(1000, nextRetryAt - current)
    };
  }

  function findCatalogLink(pattern, fallback) {
    var link = Array.from(document.querySelectorAll('a[href]')).find(function (element) {
      return pattern.test(actionText(element));
    });
    return link ? absolute(link.href) : absolute(fallback);
  }

  function handleStudy(retry) {
    retry = Number(retry || 0);
    if (selectStudyYear()) {
      setState({ phase: 'study', message: '正在切换到 ' + policy.year + ' 年学习记录' });
      setTimeout(function () { if (route() === 'study') handleStudy(retry + 1); }, 700);
      return;
    }
    var info = scanStudy();
    if (!info.ready) {
      if (retry < 30) {
        setState({ phase: 'study', message: '等待学习记录加载（' + (retry + 1) + '/30）' });
        setTimeout(function () { if (route() === 'study') handleStudy(retry + 1); }, 500);
      } else {
        setState({ running: false, phase: 'study', message: '学习记录加载超时，请刷新后继续' });
      }
      return;
    }
    var plan = Core.buildAnnualPlan(info.records, state.catalogRecords || [], policy);
    var summary = plan.summary;
    setState({
      credit: summary.totalEarned,
      publicEarned: summary.publicEarned,
      otherEarned: summary.otherEarned,
      publicProjected: plan.projectedPublic,
      otherProjected: plan.projectedOther,
      studyRecords: info.records,
      planTasks: simplifyTasks(plan.tasks),
      phase: 'study',
      message: '公需 ' + summary.publicEarned + '/' + policy.publicTarget +
        '，其他 ' + summary.otherEarned + '/' + policy.otherTarget
    });
    log('[年度计划] ' + policy.year + ' 已获公需 ' + summary.publicEarned + '，其他 ' + summary.otherEarned +
      '；计划后公需 ' + plan.projectedPublic + '，其他 ' + plan.projectedOther + '；任务 ' + plan.tasks.length + ' 项');
    if (!state.running) return;
    if (summary.satisfied) {
      setState({ running: false, phase: 'done', message: policy.year + ' 年目标已完成：公需 ' +
        policy.publicTarget + ' 分与其他 ' + policy.otherTarget + ' 分均达标' });
      return;
    }
    var blockedApplications = state.blockedApplicationYear === Number(policy.year) &&
      Array.isArray(state.blockedApplications) ? state.blockedApplications : [];
    var task = plan.tasks.find(function (item) {
      return item.type === 'apply' && item.record.url &&
        blockedApplications.indexOf(item.record.url) < 0;
    });
    if (task) {
      setState({
        currentCourseUrl: task.record.url,
        currentCourseName: task.record.name,
        phase: task.type === 'apply' ? 'certificate' : 'course',
        message: task.reason + '：' + task.record.name
      });
      navigate(task.record.url);
      return;
    }
    var localApply = info.localTasks.find(function (item) {
      return item.record.status === 'completed' &&
        blockedApplications.indexOf(item.record.url) < 0 &&
        /申请证书|申请学分|确认/.test(actionText(item.element));
    });
    if (localApply) {
      setState({
        currentCourseUrl: localApply.record.url,
        currentCourseName: localApply.record.name,
        phase: 'certificate',
        message: '申请学分：' + localApply.record.name
      });
      setTimeout(function () { if (state.running) humanClick(localApply.element); }, 600);
      return;
    }
    task = plan.tasks.find(function (item) { return item.type === 'resume' && item.record.url; });
    if (task) {
      setState({
        currentCourseUrl: task.record.url,
        currentCourseName: task.record.name,
        phase: 'course',
        message: task.reason + '：' + task.record.name
      });
      navigate(task.record.url);
      return;
    }
    var blockedApplyTasks = plan.tasks.filter(function (item) {
      return item.type === 'apply' && item.record.url &&
        blockedApplications.indexOf(item.record.url) >= 0;
    });
    if (blockedApplyTasks.length) {
      var blockedDecision = blockedApplicationDecision(
        blockedApplyTasks, state.blockedApplicationRetryAt, Date.now(), state.cardRetryQueue
      );
      if (blockedDecision.action === 'retry') {
        var retryTask = blockedDecision.task;
        setState({
          running: true,
          paused: false,
          blockedApplications: blockedApplications,
          blockedApplicationRetryAt: blockedDecision.retryAt,
          cardRetryQueue: blockedDecision.queue,
          currentCourseUrl: retryTask.record.url,
          currentCourseName: retryTask.record.name,
          phase: 'certificate',
          message: '定时复查培训卡并重试学分申请：' + retryTask.record.name
        });
        navigate(retryTask.record.url);
        return;
      }
      var waitMinutes = Math.max(1, Math.ceil(blockedDecision.waitMs / 60000));
      setState({
        running: true,
        paused: false,
        phase: 'card',
        message: '其他学习任务已处理；' + blockedApplyTasks.length +
          ' 门已完成课程等待培训卡，约 ' + waitMinutes + ' 分钟后自动复查'
      });
      setTimeout(function () {
        if (state.running && route() === 'study') handleStudy(0);
      }, Math.min(blockedDecision.waitMs, 2147483647));
      return;
    }
    var needsPublic = plan.projectedPublic < Number(policy.publicTarget);
    setState({ phase: 'catalog', catalogSourcesVisited: [], message: needsPublic ? '寻找公需课' : '在继续教育与全员专项中寻找其他课程' });
    navigate(needsPublic ?
      findCatalogLink(/公需|公共必修|公共科目/, '/cme/index.html') :
      findCatalogLink(/继续教育|继教/, '/cme/index.html'));
  }

  function scanCoursewares() {
    var nodes = uniqueElements(Array.from(document.querySelectorAll(
      'a.cw-title-link[href*="cwid="],a[href*="course_ware.aspx?cwid="],' +
      '.course[data-href*="cwid="],[class*="course-item"][data-href*="cwid="],[onclick*="cwid="],' +
      '.lis-inside-content[onclick*="course_ware"],.lis-inside-content h2[onclick*="course_ware"],h2[onclick*="course_ware"]'
    )));
    return nodes.map(function (node) {
      var url = node.href || node.getAttribute('data-href') || extractActionUrl(node);
      var box = node.closest('.course, tr, .lis-inside-content, [class*="course-item"]') || node.parentElement;
      var title = node.matches('a,h2') ? node : node.querySelector('a.cw-title-link,h2,[class*="title"]');
      var text = clean(box ? box.innerText || box.textContent : node.textContent);
      var status = /待考试/.test(text) ? '待考试' : (/已完成/.test(text) ? '已完成' : (/学习中/.test(text) ? '学习中' : (/互动/.test(text) ? '互动' : '未学习')));
      var fullUrl = absolute(url);
      return { name: clean(title ? title.textContent : node.textContent), url: fullUrl, cwid: queryParam('cwid', fullUrl), status: status };
    }).filter(function (item, index, all) { return item.cwid && all.findIndex(function (other) { return other.cwid === item.cwid; }) === index; });
  }

  function mergeRecords(left, right) {
    var map = {};
    (left || []).concat(right || []).forEach(function (record) {
      var normalizedRecord = Core.normalizeRecord(record, policy);
      var key = normalizedRecord.id || normalizedRecord.url || normalize(normalizedRecord.name);
      if (!key) return;
      map[key] = Object.assign({}, map[key] || {}, normalizedRecord);
    });
    return Object.keys(map).map(function (key) { return map[key]; });
  }

  function scanCourseDetail() {
    var text = pageText();
    var url = location.href;
    var titleNode = document.querySelector('h1,h2,.course-title,.project-title,[class*="course"][class*="name"],[class*="project"][class*="name"]');
    var action = findSemanticActions(/立即学习|开始学习|加入学习|报名|选课|选择培训卡|使用培训卡|进入学习|继续学习/, /取消|退出|返回/, {
      maxText: 24,
      selectors: 'a[href],button,input[type="button"],input[type="submit"],[role="button"],[onclick],[data-href]'
    })[0];
    return {
      record: Core.normalizeRecord({
        id: queryParam('cid', url) || queryParam('cwid', url) || clean(titleNode && titleNode.textContent),
        name: clean(titleNode ? titleNode.textContent : state.currentCourseName),
        url: url,
        year: Core.parseYear(text, policy.year),
        creditText: text,
        durationText: text,
        categoryText: text,
        sourceText: text,
        status: 'not_started',
        enrolled: false,
        text: text
      }, policy),
      action: action
    };
  }

  function processUnenrolledCourse(detail) {
    var catalog = mergeRecords(state.catalogRecords || [], [detail.record]);
    var plan = Core.buildAnnualPlan(state.studyRecords || [], catalog, policy);
    var selected = plan.tasks.find(function (task) {
      return task.type === 'enroll' && (task.record.id === detail.record.id || task.record.url === detail.record.url);
    });
    setState({
      catalogRecords: catalog,
      planTasks: simplifyTasks(plan.tasks),
      publicProjected: plan.projectedPublic,
      otherProjected: plan.projectedOther
    });
    if (!selected) {
      setState({ phase: 'catalog', message: '已记录课程信息，继续寻找更优组合' });
      setTimeout(function () { navigate('/cme/index.html'); }, 800);
      return true;
    }
    if (!detail.action || !detail.action.element) return false;
    setState({
      phase: 'enroll',
      currentCourseName: detail.record.name,
      currentCourseUrl: detail.record.url,
      message: selected.reason + '：' + detail.record.name
    });
    setTimeout(function () {
      if (!state.running) return;
      var actionUrl = extractActionUrl(detail.action.element);
      if (actionUrl) navigate(actionUrl);
      else humanClick(detail.action.element);
    }, 900);
    return true;
  }

  function handleCourse(retry) {
    retry = Number(retry || 0);
    var items = scanCoursewares();
    if (!state.running) return;
    if (!items.length) {
      var detail = scanCourseDetail();
      if (detail.action && detail.record.credit > 0 && processUnenrolledCourse(detail)) return;
      if (retry < 30) {
        setState({ phase: 'course', message: '等待课件列表加载（' + (retry + 1) + '/30）' });
        setTimeout(function () { if (route() === 'course') handleCourse(retry + 1); }, 500);
      } else {
        var handled = processUnenrolledCourse(detail);
        if (!handled) setState({ running: false, phase: 'course', message: '未识别课件或选课入口，请查看页面日志' });
      }
      return;
    }
    log('[课程] 识别到 ' + items.length + ' 个课件');
    var pending = items.find(function (item) { return item.status === '待考试'; });
    if (pending) {
      setState({ currentCwid: pending.cwid, phase: 'exam', message: '考试：' + pending.name });
      navigate('/pages/exam.aspx?cwid=' + encodeURIComponent(pending.cwid));
      return;
    }
    var next = items.find(function (item) { return item.status !== '已完成'; });
    if (next) {
      setState({ currentCwid: next.cwid, phase: next.status === '互动' ? 'case' : 'player', message: '学习：' + next.name });
      navigate(next.url);
      return;
    }
    setState({ phase: 'study', message: '本项目课件已完成，核验学分' });
    navigate('/pages/study_info_list.aspx');
  }

  function playerMediaStatus() {
    var video = document.querySelector('video');
    var bridge = null;
    try {
      bridge = pageWindow.player && typeof pageWindow.player.j2s_getDuration === 'function' ? pageWindow.player : null;
    } catch (_) {}
    var current = 0;
    var duration = 0;
    if (video) {
      current = Number(video.currentTime || 0);
      duration = Number(video.duration || 0);
    } else if (bridge) {
      try {
        current = Number(bridge.j2s_getCurrentTime() || 0);
        duration = Number(bridge.j2s_getDuration() || 0);
      } catch (_) {}
    }
    if (!isFinite(current)) current = 0;
    if (!isFinite(duration)) duration = 0;
    return {
      found: Boolean(video || bridge),
      video: video,
      bridge: bridge,
      paused: Boolean(video && video.paused),
      ended: Boolean((video && video.ended) || (duration > 0 && current >= duration - 1)),
      current: current,
      duration: duration
    };
  }

  function parseClassroomQuiz() {
    var modal = document.querySelector('.pv-ask-modal-wrap');
    if (!modal || !visible(modal) || /(^|\s)pv-hide(\s|$)/.test(classText(modal))) return null;
    var questionNode = modal.querySelector('.pv-ask-right,[class*="question"],.question');
    var question = clean(questionNode ? questionNode.textContent : '');
    var options = Array.from(modal.querySelectorAll('.pv-ask-form input[type="radio"],input[type="radio"]')).map(function (input) {
      var label = input.closest('label') || input.parentElement;
      return {
        input: input,
        value: clean(input.value),
        text: clean(label ? label.textContent : input.value).replace(/^[A-E][、.．)\s]*/, '')
      };
    });
    var result = /(^|\s)pv-ask-modal-answer(\s|$)/.test(classText(modal)) ||
      /回答正确|回答错误|答对|答错/.test(clean(modal.textContent));
    var signal = clean(modal.textContent);
    var correct = modal.querySelector('.pv-right-icon,[class*="correct"],[class*="right-icon"]') ? true :
      (modal.querySelector('.pv-wrong-icon,[class*="wrong"],[class*="error-icon"]') ? false :
        (/回答正确|答对/.test(signal) ? true : (/回答错误|答错/.test(signal) ? false : null)));
    var submit = modal.querySelector('.pv-ask-submit,button[type="submit"],input[type="submit"]');
    var close = modal.querySelector('.pv-ask-skip,.pv-ask-close,[class*="close"]') ||
      Array.from(modal.querySelectorAll('button,input[type="button"],a')).find(function (element) {
        return /继续观看|关闭|继续学习|知道了|确定/.test(actionText(element));
      });
    return {
      modal: modal,
      question: question,
      key: normalize(question).slice(0, 100),
      options: options,
      result: result,
      correct: correct,
      submit: submit,
      close: close
    };
  }

  function chooseClassroomOption(quiz, memory) {
    var learned = read(ANSWER_KEY, {});
    var known = learned['classroom:' + quiz.key];
    var tried = Array.isArray(memory.tried) ? memory.tried.slice() :
      quiz.options.slice(0, Number(memory.attempt || 0)).map(function (option) { return option.text; });
    if (known) {
      var knownKey = normalize(known);
      var exact = quiz.options.find(function (option) {
        return normalize(option.text) === knownKey && tried.indexOf(option.text) < 0;
      });
      if (exact) return exact;
    }
    return quiz.options.find(function (option) { return tried.indexOf(option.text) < 0; }) || null;
  }

  function handleClassroomQuiz() {
    var quiz = parseClassroomQuiz();
    if (!quiz) return false;
    if (window.__HY8_CLASSROOM_BUSY) return true;
    var allMemory = read(CLASSROOM_KEY, {});
    var memory = allMemory[quiz.key] || { attempt: 0, submitted: '', tried: [] };
    if (quiz.result) {
      var learned = read(ANSWER_KEY, {});
      if (quiz.correct === true && quiz.key && memory.submitted) {
        learned['classroom:' + quiz.key] = memory.submitted;
        write(ANSWER_KEY, learned);
        delete allMemory[quiz.key];
        setState({ phase: 'player', message: '课堂问答正确，继续学习' });
      } else {
        delete learned['classroom:' + quiz.key];
        write(ANSWER_KEY, learned);
        if (!Array.isArray(memory.tried)) memory.tried = [];
        if (memory.submitted && memory.tried.indexOf(memory.submitted) < 0) memory.tried.push(memory.submitted);
        memory.attempt = memory.tried.length;
        allMemory[quiz.key] = memory;
        setState({ phase: 'player', message: '课堂问答准备下一答案组合' });
      }
      write(CLASSROOM_KEY, allMemory);
      window.__HY8_CLASSROOM_BUSY = true;
      setTimeout(function () {
        if (quiz.close && enabled(quiz.close)) humanClick(quiz.close);
        else quiz.modal.classList.add('pv-hide');
        setTimeout(function () { window.__HY8_CLASSROOM_BUSY = false; }, 700);
      }, 500);
      return true;
    }
    if (!quiz.options.length || !quiz.submit) return true;
    if ((memory.tried || []).length >= quiz.options.length || Number(memory.attempt || 0) >= quiz.options.length) {
      setState({ running: false, paused: true, phase: 'player', message: '课堂问答所有选项均已尝试，请检查题目' });
      return true;
    }
    var choice = chooseClassroomOption(quiz, memory);
    if (!choice) return true;
    memory.submitted = choice.text;
    if (!Array.isArray(memory.tried)) memory.tried = [];
    if (memory.tried.indexOf(choice.text) < 0) memory.tried.push(choice.text);
    memory.attempt = Math.max(Number(memory.attempt || 0), memory.tried.length - 1);
    allMemory[quiz.key] = memory;
    write(CLASSROOM_KEY, allMemory);
    window.__HY8_CLASSROOM_BUSY = true;
    setState({ phase: 'player', message: '课堂问答：第 ' + (Number(memory.attempt || 0) + 1) + ' 个答案' });
    humanClick(choice.input);
    if (!choice.input.checked) {
      choice.input.checked = true;
      choice.input.dispatchEvent(new Event('change', { bubbles: true }));
    }
    setTimeout(function () {
      if (state.running && enabled(quiz.submit)) humanClick(quiz.submit);
      setTimeout(function () { window.__HY8_CLASSROOM_BUSY = false; }, 700);
    }, 700);
    return true;
  }

  function findPlayerPrompt() {
    var exactSelectors = [
      '#div_preview1 #btnPay',
      '#div_preview2 .rig_btn',
      '#div_tip .rig_btn',
      '.study_diaog .btn_sign',
      '#div_processbar_tip .rig_btn',
      '.pv-ask-skip',
      '.signBtn',
      '.btn_sign',
      '.study_box button',
      '.layer_tips .colse_btn',
      'button[onclick="closeProcessbarTip()"]',
      '.el-message-box__btns .el-button--primary',
      '.el-dialog__footer .el-button--primary'
    ];
    var exact = exactSelectors.map(function (selector) {
      return document.querySelector(selector);
    }).find(enabled);
    if (exact) return { element: exact, text: actionText(exact) || '继续' };
    return findSemanticActions(/继续学习|继续播放|开始播放|签到|我知道了|确定|确认|关闭提示/, /购买|支付|退出|返回|取消/, {
      maxText: 18,
      selectors: 'button,input[type="button"],a[href],[role="button"],[onclick]'
    })[0] || null;
  }

  function needsTrustedPlayerClick(element) {
    return Boolean(element && element.matches && (
      element.matches('.layer_tips .rig_btn,.study_diaog .btn_sign') ||
      element.closest('.layer_tips') && element.matches('.rig_btn') ||
      element.closest('.study_diaog') && element.matches('.btn_sign')
    ));
  }

  function handlePlayer() {
    if (!state.running) return;
    var cwid = queryParam('cwid');
    setState({ currentCwid: cwid || state.currentCwid, phase: 'player', message: '网站原生播放器正常学习中' });
    var count = 0;
    var lastPrompt = null;
    var lastPromptAt = 0;
    var lastResumeAt = 0;
    var lastMediaCurrent = -1;
    var lastMediaProgressAt = Date.now();
    if (window.__HY8_TIMER) clearInterval(window.__HY8_TIMER);
    window.__HY8_TIMER = setInterval(function () {
      if (!state.running || state.paused) { clearInterval(window.__HY8_TIMER); return; }
      count++;
      var bodyText = pageText();
      var pluginNotice = /系统检测到此浏览器安装了异常插件/.test(bodyText);
      var quizActive = handleClassroomQuiz();
      var media = playerMediaStatus();
      if (media.current > lastMediaCurrent + 0.25) {
        lastMediaCurrent = media.current;
        lastMediaProgressAt = Date.now();
      }
      if (!quizActive && media.video && !media.ended) {
        try {
          media.video.muted = true;
          media.video.volume = 0;
          if (media.video.paused) {
            var playing = media.video.play();
            if (playing && typeof playing.catch === 'function') playing.catch(function () {});
          }
        } catch (_) {}
      }
      if (!quizActive && media.bridge && !media.ended && Date.now() - lastResumeAt > 8000) {
        try {
          if (typeof media.bridge.j2s_resumeVideo === 'function') media.bridge.j2s_resumeVideo();
          lastResumeAt = Date.now();
        } catch (_) {}
      }
      var prompt = quizActive ? null : findPlayerPrompt();
      if (prompt && (prompt.element !== lastPrompt || Date.now() - lastPromptAt > 4000)) {
        lastPrompt = prompt.element;
        lastPromptAt = Date.now();
        if (needsTrustedPlayerClick(prompt.element)) {
          var trustedMessage = '等待 Hermes 处理受保护播放器提示：' + prompt.text;
          if (state.message !== trustedMessage) setState({ phase: 'player', message: trustedMessage });
        } else {
          log('[播放器] 处理提示：' + prompt.text);
          humanClick(prompt.element);
        }
      }
      if (pluginNotice && Date.now() - lastMediaProgressAt > 180000) {
        clearInterval(window.__HY8_TIMER);
        setState({
          running: false,
          paused: true,
          phase: 'blocked',
          message: '播放器兼容提示持续 3 分钟且进度未变化，交由监督器重启'
        });
        return;
      }
      var exam = document.getElementById('jrks');
      if (cwid && enabled(exam)) {
        clearInterval(window.__HY8_TIMER);
        var before = location.href;
        humanClick(exam);
        setTimeout(function () {
          if (location.href === before && route() === 'player') {
            navigate('/pages/exam.aspx?cwid=' + encodeURIComponent(cwid));
          }
        }, 1200);
        return;
      }
      if (media.found) {
        var progress = media.duration > 0 ? Math.min(100, Math.round(media.current / media.duration * 100)) : 0;
        var mediaMessage = media.ended ? '课件播放结束，等待考试入口' : '原生播放中 ' + progress + '%';
        if (state.message !== mediaMessage) setState({ phase: 'player', message: mediaMessage });
      }
      if (count >= 43200) { clearInterval(window.__HY8_TIMER); setState({ running: false, message: '播放器观察超时' }); }
    }, 1000);
  }

  var VERIFIED = [["“循之累累然乃刺之”出自（）","《素问·刺腰痛论》","B"],["“脏腑别通”之理论源于（）","《医学入门 ·脏腑相通篇》","E"],["（）是肺部疾病中最容易获得的标本之一其蛋白质谱的变化能够反映肺损伤及修复过程","肺泡灌洗液","A"],["《中国阿尔茨海默病痴呆诊疗指南（2020年版）》指出ad药物治疗的核心原则不包括？","根治疾病"],["2007年iwg-1标准中诊断可能的ad的核心要求是？","早期显著情景记忆损伤+至少1项支持性特征"],["2017cds指南建议tc_____ldl-c（合并ascvd）________未合并ascvd者_______","＜4.5mmol/L，＜1.8mmol/L，＜2.6 mmol/L","B"],["2020ada指南建议对于有迫切最少体重增加或减重需求患者推荐减重效力良好的_____或______","GLP-1RA，SGLT2i","D"],["2024年iwg-4标准中将认知功能正常但具有特定生物标志物模式的个体定义为？","AD高风险人群"],["2型糖尿病防治指南中除血糖和糖化血红蛋白的达标外理想血压是（）","＜130/80mmHg","B"],["3~13岁ecmo患者蛋白质需求为（）","1.5-2.0g/（kg.d）","A"],["阿尔茨海默病（ad）患者在功能性磁共振成像（fmri）检查中实施学习和回忆任务时的典型表现为？","额叶前区和颞叶内侧皮质激活区域缩小，信号强度降低"],["阿尔茨海默病的典型病理改变不包括以下哪项？","路易小体"],["阿尔茨海默病的非药物治疗中不属于心理治疗的是？","音乐疗法","B"],["阿尔茨海默病全病程管理的核心是？","以单个病人为中心的照护模式","B"],["柏林标准中ards的严重程度分级为（）","轻度、中度和重度","B"],["爆发性心肌炎患者合并ⅲ度房室传导阻滞应（）","临时起搏器+ECMO","A"],["泵前压力的影响因素不含（）","血压","E"],["鼻窦炎以（）最常见","上颌窦","A"],["鼻渊的别名不包括（）","脑透","E"],["不属于其他方式导致血小板减少的是","Antiplatelet antibodies (APA) -","E"],["餐时注射的短效或速效胰岛素最好选择的部位是（）","腹部","A"],["常用气道廓清技术包括以下哪些（）","指导性咳嗽、主动循环呼吸技术、自主引流、呼气末正压/振动呼气末正压、高频胸壁振荡、体位引流、运动/活动、手法辅助咳嗽、机械式吸入呼出装置、气道抽吸","A"],["撤机前的关键评估不包含的项目是（）","感染","E"],["痴呆临床表现的abc症状中a代表的是？","日常生活能力下降"],["磁共振波谱成像（mrs）中ad最早期的波谱变化为？","NAA/Cr 比值降低，MI/Cr 比值升高"],["单一出现时需与颞叶癫痫或颞叶器质性损害鉴别的幻觉类型是？","幻嗅"],["胆经的穴位不包括（）","地仓透颊车","D"],["导致血浆ad生物标志物检测存在挑战的因素不包括？","单一样本可检测多项标志物"],["典型ad患者最早出现的临床症状是哪种记忆障碍？","情景记忆障碍"],["调节炎症反应的重要因子是（）","MicroRNA（miRNA）","C"],["对于cv风险更高（已有ascvd或10年ascvd风险≥15%）的糖尿病合并高血压患者若能安全达到血压目标为()可能是合适的","＜130/80 mmHg","A"],["多因素分析显示高tg导致2型糖尿病患者血糖控制不佳风险增加（）倍","1.63","B"],["耳胀的病因病机不包括（）","肝阳上亢","E"],["肺泡巨噬细胞（am）是肺部最丰富的免疫细胞且在ards的发生发展中扮演重要角色am作用不包括（）","促进损伤","E"],["俯卧位通气的主要作用不包括以下哪一项（）","导致肺泡塌陷","D"],["俯卧位通气时最常见的不良反应是（）","低血压","A"],["根据“脏腑别通”错误的是（）","肝与命门相通","C"],["根据2014年iwg-2标准阿尔茨海默病（ad）相关的生物标志物被明确分为哪两类？","诊断性标志物和进展性标志物"],["根据2026年ada糖尿病诊疗标准对于已确诊ascvd或高风险的t2dm患者在降糖药物选择上应优先考虑哪类药物？","GLP-1RA或SGLT2i","A"],["根据nia-aa2011版诊断标准下列哪项不属于“很可能的ad痴呆”核心临床标准中的早期突出认知损害表现？","早期即出现显著的人格改变和行为异常"],["共病管理模式的要素不包括以下哪项？","完全依赖社区志愿者管理","E"],["关于《黄帝内经》开阖枢理论错误的是（）","太阳为阖","E"],["关于2020ada指南以下表述正确的是（）","对于具有高危或已确诊的冠脉粥样硬化性心血管疾病（ASCVD）的患者，不论HbA1c水平如何，均推荐具有CV获益的GLP-1 RA或SGLT2i","C"],["关于阿尔茨海默病（ad）的临床表现三联征以下哪项描述最为准确？","认知障碍（Cognition）、精神行为症状（BPSD）、日常生活能力受损（ADL）"],["关于泵控逆流试验的描述不正确的是（）","保持ECMO通气","C"],["关于肝性脑病下列说法不正确的是？","前期出现昏迷"],["关于膈肌肌电图（emg）在膈肌疲劳时的表现下列说法正确的是（）","低频成分（30~50Hz）增加，高频成分（130~250Hz）减少","C"],["关于过量异位脂肪与t2dm患者cvd风险的关系以下描述正确的是？","心血管及心肾疾病风险的加速上升往往在明显T2DM出现之前就已开始","C"],["关于基层胰岛素治疗患者的转诊情况描述不正确的是（）","基层医院应用胰岛素治疗不安全，建议转上级医院","E"],["关于基因检测的推荐意见以下哪项是正确的？","有明确痴呆家族史的患者应进行基因检测（A级推荐）"],["关于简易精神状态检查（mmse）的缺点下列说法错误的是？","不受教育程度影响"],["关于认知障碍的病因分类以下哪项描述符合变性性认知障碍的特点？","隐匿起病，进行性加重，症状不可逆"],["关于行为变异型额颞叶痴呆不属于核心临床表现-行为症状的是？","找词困难和命名障碍"],["关于血管性认知障碍（vci）的newcastle分型下列哪项描述正确？","VI型指伴AD的脑血管病变"],["关于阻塞性睡眠呼吸暂停（osa）对ad的影响以下哪项错误？","AD患者对CPAP治疗的耐受性极好","E"],["关于ad患者共病的流行病学特点说法正确的是？","65 岁以上老年痴呆患者共病发生率更高","D"],["关于ad血液生物标志物以下哪项描述是正确的？","家族性AD患者血浆总Aβ或Aβ42水平增高"],["关于ad诊断中生物标志物的应用原则以下哪项是错误的？","生物标志物阳性即可确诊AD，无需结合临床症状"],["关于ad治疗药物的用药安全以下哪项是正确的？","多奈哌齐若出现肝肾功能损害，应考虑减量或停药","C"],["关于apacheii评分描述有误的是（）","是专门为ARDS设计的","B"],["关于ards患者的液体管理以下哪项说法是正确的（）","限制液体管理可减少呼吸机依赖时间","B"],["关于ards患者使用ecmo的适应证以下错误的是（）","ARDS病程超过3个月仍未改善","C"],["关于aβ的清除途径以下说法正确的是？","脑部主要通过蛋白酶降解、血脑屏障清除等途径清除"],["关于bpsd的临床特点以下表述错误的是？","精神症状仅在痴呆中晚期出现"],["关于bpsd诊治的误区说法错误的是？","重视社会心理干预的核心作用"],["关于ecmo静脉引流不畅的原因描述不正确的是（）","出血","C"],["关于gip和glp-1受体在中枢神经系统的分布以下描述正确的是？","均存在于大脑中负责调控能量平衡的区域，但分布位置不完全相同","A"],["关于pdi和pdimax的描述正确的是（）","Pdi波形峰值时间是早期疲劳的敏感指标","C"],["关于plt生理描述错误的是（）","(100 ~ 400) x 10 9 / L","E"],["国内指南指出ad伴发精神行为症状的药物干预核心原则为？","抗痴呆治疗为基本，必要时使用精神药物"],["过量异位脂肪在t2dm患者cvd发生中起\"上游\"作用其导致cvd风险升高的主要途径是？","通过升高血压和导致血脂水平异常来增加CVD风险","B"],["缓解2型糖尿病的方法有（）","以上皆是","D"],["荟萃分析显示患有黄斑水肿的糖尿病患者的（）水平较高","血清甘油三酸酯","A"],["火针的使用注意事项下面描述错误的是","宁深勿浅","A"],["基础胰岛素通常在什么时间注射（）","睡前注射","C"],["急性肺动脉栓塞的典型影像学表现为（）","肺动脉充盈缺损","A"],["急性肺动脉栓塞患者通常不会出现以下哪种症状（）","端坐呼吸","D"],["间充质基质细胞（mscs）的主要来源不包括以下哪一项（）","心脏组织","D"],["间质性肺疾病（ild）的主要病理生理机制是（）","慢性炎症及纤维化重构","B"],["尽管糖尿病的综合管理水平不断提高以下哪种疾病仍然是2型糖尿病患者最主要的死亡原因（）","心血管疾病","C"],["进行内侧颞叶萎缩评估（mta）的标准影像学层面为？","冠状位 T1WI，脑桥前面水平通过海马体部层面"],["经颅直流电刺激（tdcs）的阳极刺激作用是？","增强刺激部位神经元的兴奋性"],["康复的目标不正确的是（）","关节肌肉功能恢复可以慢点","B"],["空腹血糖的值是（）","4.4~7.0","A"],["六种火针的选择法中一般疾病均以哪种火针为主","细火针","A"],["路易体痴呆（dlb）的bpsd核心特点是？","幻觉早而明显"],["毛细血管渗漏综合征(capillaryleaksyndromecls)的病因有不正确的是（）","上呼吸感染","D"],["美金刚显著预防ad患者以下哪项bpsd症状的发生？","激越/攻击"],["膜前压力不宜超过多少（）mmhg","300","E"],["脑脊液标本处理中受检测前因素影响最大的标志物是？","Aβ42"],["溺水者继发感染最常见的病原体是（）","铜绿假单胞菌","B"],["溺水者ecmo的适用条件是（）","PaO?/FiO?<100mmHg且常规通气无效","A"],["评估mda5-dm治疗反应的关键检查是（）","高分辨率CT（HRCT）肺窗","B"],["轻度认知障碍（mci）诊断的核心标准不包括？","符合痴呆的诊断标准","D"],["清醒ecmo的禁忌症不包括（）","发热","D"],["清醒ecmo的特点不包括()","更安全","D"],["人体第五大生命体征是（）","疼痛","A"],["认知障碍自评量表（ad8）的评分标准中提示需进一步临床评估的总分阈值是？","≥2 分"],["妊娠合并急性上呼吸道感染终止妊娠的指征不包括（）","胎儿畸形","D"],["如果ecmo患者排除了躁动、出血、血管扩张、张力性气胸、心包填塞、腹腔高压、管路凝血或移位、容量不够等还是引流不够建议如何做（）","增加插管","E"],["生理状态下白色脂肪组织在脂质代谢中的关键功能是？","作为膳食甘油三酯的关键''缓冲区''","B"],["失读的病变部位主要是？","顶下小叶的缘上回和角回"],["实施ecmo患者导致肠道缺血缺氧的原因不包括（）","冠状动脉粥样硬化","B"],["使用胰岛素笔推注完毕后保持负压的针头停留至少（）后再拔出","10s","D"],["属于强效高选择性5-羟色胺（5-ht）再摄取抑制剂的ad治疗药物是？","帕罗西汀"],["糖尿病并发症或相关疾病中患病率最高的是（）","高血压","C"],["糖尿病患者死亡的最主要原因是（）","大血管并发症","B"],["糖尿病患者运动的禁忌症错误的是（）","体重超重的2型糖尿病患者","C"],["糖尿病运动原则（）","以上都是","D"],["体育锻炼对ad患者bpsd的影响是？","显著改善抑郁症状，整体BPSD无显著改善"],["我国现阶段早发2型糖尿病有哪些代谢指标异常（）","以上皆是","D"],["我国t2dm患者合并心血管代谢危险因素的情况以下哪项描述正确？","我国T2DM患者中约六成合并高血压","C"],["下列关于2型糖尿病正确的是（）","应激情况下可发生酮症酸中毒","B"],["下列关于锋勾针的说法正确的是","以上全是","E"],["下列关于喉痹说法有误的是（）","病程短","D"],["下列哪个不是影响act缩短的因素（）","DIC","D"],["下列哪项不是ards的早期症状（）","胸痛","D"],["下列哪项不属于ad的脑相关危险因素？","2型糖尿病"],["下列哪一项不是ards的评估工具（）","GCS昏迷评分","C"],["下列属于清醒ecmo低危组的是（）","能够站立行走","E"],["下面哪个不是ecmo抗凝常见的监测指标（）","ALT","E"],["下面哪一项不是ards的肺内病因（）","重症急性胰腺炎","E"],["相较于pet影像脑脊液生物标志物的优势不包括?","无创性检测"],["心源性肺水肿患者通常不会出现以下哪种症状（）","呼吸困难进行性加重","D"],["新九针的特性包括","以上都是","D"],["新九针发明者是","师怀堂","B"],["新九针改革创新的过程中下面哪种针没有改动过","三棱针","A"],["新九针和古九针的区别下面哪一项是错误的","长针改成火针","B"],["新九针之火针是使用哪种金属制成的","钨","C"],["血栓插管侧肢体循环异常正确的处理措施是（）","预防血栓抗凝是关键，预防血液瘀滞","B"],["胰岛素吸收最快的部位（）","腹部","D"],["胰腺炎合并腹腔高压（iah）时首要处理是（）","降低潮气量至4-6ml/kg","B"],["以下表述正确的是（）","通过综合管理，纠正代谢紊乱，可预防T2D慢性并发症，改善远期结局","A"],["以下不是ali/ards主要病理特征的是（）","肺容积增高","B"],["以下不属于预防和治疗血管渗漏的新方法的是（）","通过去除细胞间连接防止血管渗漏——Wnt及其同源物R-spondin除了调控胚胎发育外，还可用于治疗炎症相关疾病","B"],["以下不属于ad患者精神病性症状的是？","抑郁"],["以下关于呼吸康复的描述哪一项是正确的（）","无禁忌症的ARDS患者应尽早进行呼吸康复训练","C"],["以下关于机械通气对脑损伤影响的描述哪一项是正确的（）","机械通气通过多种机制，包括炎症介质释放、颅内压变化和高碳酸血症，影响脑损伤","C"],["以下关于胰岛素的保存的叙述不正确的是（）","胰岛素注射笔应放置于冰箱内储藏","C"],["以下关于胰岛素应用的叙述错误的是（）","高血糖都可以启用胰岛素治疗","D"],["以下关于谵妄五种表型与ards预后关系的描述哪一项是正确的（）","低氧血症相关谵妄：与较高的长期认知功能障碍风险相关","C"],["以下关于ards幸存者的肺部影像学改变的描述哪一项是错误的（）","高分辨CT：主要用于评估肌萎缩","B"],["以下关于bpsd的定义描述正确的是？","认知障碍患者经常出现的紊乱的感知觉、思维内容、心境和行为症状"],["以下哪个说法是错误的（）","以上皆为正确","D"],["以下哪项不是bpsd造成的不良后果？","减慢痴呆患者认知功能下降速度"],["以下哪项不属于认知障碍诊治原则？","晚期诊断痴呆后再开展干预治疗","E"],["以下哪项不属于bpsd中的睡眠障碍类型？","易激惹"],["以下哪项是评估痴呆患者精神行为症状的常用量表？","NPI"],["以下哪项是ad的特征性萎缩表现对早发型ad尤为重要？","楔前叶+顶叶萎缩"],["以下哪项属于bpsd早期识别量表？","神经精神问卷（NPI）"],["以下哪一个不是超重和肥胖的t2dm患者进行体重管理可以得到的获益（）","改善胰岛素抵抗和α细胞功能","D"],["以下哪一项不是时间控制自适应通气（tcav）的潜在优势（）","增加呼吸机相关性肺损伤","D"],["以下哪种疾病不属于变性性认知障碍？","血管性痴呆（VaD）"],["以下哪种量表主要用于评估痴呆患者的日常生活功能？","ADL"],["以下哪种生物标志物在阿尔茨海默病患者中其水平变化早于海马萎缩和临床症状的出现？","CSF Aβ42"],["以下哪种血液生物标志物可用于预测脑内aβ病理改变和ad转化率？","血浆Aβ42/40比值"],["以下属于激越中身体非攻击性行为的是？","不恰当的处理事情"],["以下属于激越中言语攻击性行为的是？","发出怪声"],["引起血小板激活的原因错误的描述是（）","出血/血液稀释","D"],["营养支持前应评估的项目不包括（）","精神状态及行动能力","D"],["用于ad患者睡眠障碍治疗可增强脑内γ-氨基丁酸与受体结合的药物是？","地西泮"],["与神经轴索变性和损伤相关的新型体液生物标志物是？","神经丝轻链（NfL）"],["预防ecmo并发症最好的办法是（）","缩短上机时间","D"],["员利针的禁忌症包括","以上全是","E"],["缘患者于3天前开始无明显诱因出现胸痛呈胸骨后疼痛持续约10余分钟缓解6小时（6月1日上午6点左右）开始出现胸痛加重放射至双肩部伴冷汗间中心悸、气促门诊辅查心电图窦性心动过速三度房室传导阻滞非阵发性交界","1、可疑心肌梗死、冠心病、心功能III级；2、急性心肌炎？3、三度房室传导阻滞","B"],["孕产妇ards使用糖皮质激素的指征是（）","仅限病毒性肺炎合并休克","B"],["孕妇肺结核用药需调整（）","链霉素替换为乙胺丁醇","B"],["运动对于2型糖尿病患者的益处错误的是（）","降低血压","D"],["运动推荐时机（）","从吃第一口饭算起，在饭后1小时后","A"],["在2019esc/easd指南糖尿病病程≥10年且无靶器官损伤及任何其他危险因素的糖尿病患者的心血管风险分层是（）","高危","B"],["在2022年ada的指南中t2dm患者进行降糖治疗时需多久评估药物治疗方案并根据需要进行调整（）","定期（每3-6个月）","B"],["在2型糖尿病患者中有（）患者合并超重/肥胖？","约60%","B"],["在阿尔茨海默病的早期筛查中哪种影像学检查方法可以排除其他导致痴呆的原因如脑小血管病和血管性痴呆？","头部MRI"],["在病史采集中以下哪项属于“精神行为”方面的评估内容？","抑郁和焦虑、幻觉和妄想"],["在常见认知障碍疾病的临床特点对比中路易体痴呆（dlb）的典型运动症状是？","帕金森征（行动迟缓，步态异常）"],["在痴呆的神经心理学评估中用于评估患者情景记忆的常用测验是？","韦氏记忆量表个人经历分测验"],["在痴呆诊疗中用于评估患者总体退化程度的量表是？","GDS"],["在辅助检查中swi（磁敏感加权成像）主要用于明确？","微出血"],["在肝素的监测中下列（）是act的优点","实用、简便可行","D"],["在呼吸肌力量评估中以下关于最大吸气压（mip）和最大呼气压（mep）的描述哪项是正确的（）","MEP大于100cmH2O表示患者具备良好咳嗽能力","D"],["在结构影像学检查中对于痴呆诊断首选的检查手段是？","MRI"],["在为预防和治疗２型糖尿病而进行的肥胖和体重管理中ａｄａ在２０２２版的指南中推荐的咨询频率和饮食变化是（）","6个月内≥16次会话，实现５００-７５０千卡/天的能量负平衡","A"],["在严重ards患者中推荐使用下列哪种机械通气模式（）","保护性肺通气（低潮气量+高PEEP）","C"],["在英国前瞻性糖尿病研究（ukpds35）显示hba1c每下降1%的益处错误的是（）","致死及非致死卒中下降50%","B"],["在智能障碍基础上出现的与周围环境不协调的愉快体验表情单调刻板的是？","欣快"],["在ad与房颤的关系中以下哪项是两者共有的风险因素？","甲状腺功能亢进","C"],["在ards的诊断及鉴别诊断中以下哪种检查方法可用于明确肺栓塞或间质病变（）","CTPA/HRCT","C"],["在ards治疗过程中何时考虑拔管（）","自主呼吸恢复，氧合稳定，镇静停用后无明显呼吸窘迫","B"],["在ards治疗中俯卧位通气最适用于（）","重度ARDS患者（PaO?/FiO?<150mmHg）","C"],["在aβ的不同形态中哪一种被认为是神经毒性最强且与突触功能障碍关系最密切的？","可溶性Aβ寡聚体"],["在ｂｍｉ大于等于３０．０ｋｇ／ｍ２的二型糖尿病患者中以下哪种治疗方案是合适的选择（）","以上皆是","D"],["在ecmo支持过程中下列哪项措施最重要（）","早期下调机械通气参数，避免肺损伤","C"],["在fdgpet显像中典型的阿尔茨海默病（ad）代谢模式是？","枕顶叶为主，额叶其次"],["在t2dm合并心衰的患者中以下哪一类药物是在2022ada指南的治疗路径推荐中()","SGLT2i","D"],["诊断ad源性scd的核心条件不包括？","AD生物标志物阴性"],["诊治bpsd的价值不包括？","与认知障碍及日常生活能力下降互不影响"],["中国2020年ad痴呆诊疗指南中ad痴呆临床分级的金标准是？","临床痴呆评定量表（CDR）"],["中国怀堂九针（新九针）针具”这一研究成果于哪一年通过国家鉴定并已在国内外临床运用","1983年","C"],["重症患者应当在入院后最初（）内早期开始肠内营养","24-48小时","C"],["注射胰岛素的糖尿病患者运动前要注意的是（）","运动前胰岛素注射在腹部","C"],["自身免疫性脑炎（ae）导致的认知障碍通常伴随下列哪组症状？","癫痫、精神障碍"],["ad患者激越发生率随cdr分期变化的趋势是？","逐渐升高"],["ad患者在磁共振波谱成像（mrs）检查中最早期的波谱变化为？","NAA/Cr 比值降低，MI/Cr 比值升高"],["ad疾病修饰疗法（dmt）的核心目的是？","阻断或延缓疾病的进展"],["ad临床症状出现前β淀粉样蛋白（aβ）开始沉积的时间为？","10~20年"],["ad临床症状的abc分类中a代表的是？","日常生活能力受损"],["ad脑结构变化的典型特点是？","早期海马萎缩，晚期全脑萎缩"],["ad诊断后支持管理的五大目标不包括？","立即实现认知功能完全恢复","C"],["ada指南建议对于有迫切最少体重增加或减重需求患者推荐减重效力良好的_____或______","α-糖苷酶抑制剂，二甲双胍","C"],["ards柏林标准哪项是错误的（）","轻度：PEEP≥5cmH2O时，100mmHg＜PaO2/FiO2≤200mmHg","D"],["ards柏林定义的局限性不包括（）","低氧血症标准需要明确","A"],["ards的病理生理基础不包括以下哪一项（）","肺动脉高压","D"],["ards的发生通常是由多种因素引起的肺损伤属于间接损伤的是（）","输血反应","B"],["ards的发生通常由多种因素引起的肺损伤包括直接性肺损伤和间接性肺损伤下列哪个属于直接性肺损伤（）","肺炎、吸入毒物、肺挫伤、溺水","D"],["ards患者的最佳营养支持策略是（）","适量肠内营养为主，肠外营养为辅","B"],["ards患者机械通气的首要目标是（）","避免呼吸机相关肺损伤（VILI）","C"],["ards患者精神障碍的高发病率与以下哪项因素无关（）","ARDS的严重程度","E"],["ards患者早期最常见的病理生理改变是（）","肺泡水肿","B"],["ards特征性病理变化是（）","肺血管内皮和肺泡损害，肺间质水肿","D"],["ards形态学差异的最直接证据是（）","胸部CT","A"],["ards炎症导致脑损伤的主要机制是（）","炎症介质通过全身循环进入大脑，导致神经元凋亡和海马体损伤","C"],["ards液体管理的目标是（）","适度限制液体，以减少肺水肿","C"],["ards与间质性肺疾病的鉴别要点不包括以下哪一项（）","血流动力学监测","D"],["ards与心源性肺水肿的主要鉴别点不包括以下哪一项（）","血清肌酐水平","E"],["ards预后分类评分(spires)由四个变量组成不包括（）","患者性别","B"],["ards主要的病理生理改变不包括（）","顺应性升高","E"],["aβ的主要清除部位是?","脑部"],["bpsd临床管理的dice过程不包括以下哪项？","药物干预"],["cds指南建议tc_____ldl-c（合并ascvd）________未合并ascvd者_______","以上都不正确","E"],["ｄｉｒｅｃｔ研究中体重减轻达到多少的t2dm患者达到了最佳的糖尿病缓解（）","≥１５ｋｇ","D"],["dpp-4抑制剂类药物的作用机制是？","减少肠促胰素（GIP和GLP-1）的降解，增强肠促胰素效应","A"],["ecmo撤机评估的关键指标是（）","左室射血分数>40%","A"],["ecmo的抗凝方式（）","普通肝素","B"],["ecmo的中文名字（）","体外膜肺氧合","C"],["ecmo肺脏支持成活出院率最高的一组是（）","新生儿","C"],["ecmo过程中最常用的抗凝剂是（）","肝素","C"],["ecmo患者肠内营养如果胃内残留量（）应暂时停止输注或降低输注速度","≥200ml","C"],["ecmo抗凝监测中aptt的特点是（）","准确，无床旁，肝素相关型号","C"],["ecmo期间感染的常见致病菌是（）","G-杆菌","B"],["ecmo下肢静脉血栓和肺栓塞（）","可以不用处理","B"],["ecmo血栓凝血的相关背景错误的描述是（）","接头连接处不容易出现血栓","D"],["ecmo压力监测的位点不包括（）","不需要监测","E"],["ecmo原因主要有以下几个方面错误的描述是（）","带机时间短","E"],["ecmo运行期间需注意事项不包括（）","使用血管活性药物","A"],["ecmo最常见的并发症为（）","出血","A"],["ecmo最常见的出血来源为不正确的描述为（）","皮肤","E"],["ecmo最常用的模式是（）","VA","B"],["field研究高tg、低hdl-c亚组分析发现使用非诺贝特可以降低心血管事件达（）%","27","B"],["field研究中在2型糖尿病患者中非诺贝特可降低微血管性截肢（）%","46","C"],["gip（葡萄糖依赖性促胰岛素多肽）由哪种细胞分泌？","十二指肠和空肠的K细胞","C"],["gip通过哪种机制缓解glp-1受体激动剂引起的恶心和呕吐？","通过激动后脑的GIP受体","B"],["mda5抗体阳性患者最突出的临床特征是（）","快速进展性间质性肺病（RP-ILD）","B"],["mes量表主要涵盖哪两个认知域的评估？","记忆与执行功能"],["mscs通过哪种途径抑制局部细胞因子的过度产生从而改善肺损害（）","胆碱能抗炎途径（CAP）","B"],["t2dm患者cvd死亡原因中比例最高的是？","心源性猝死","D"],["tg每升高0.5mmol/l微血管病变风险增加（）糖尿病肾病风险增加（）","16%, 23%","C"]].concat([
    ['体重正常但存在中心型肥胖', '以减少内脏脂肪沉积为主，更加关注腰围的改变'],
    ['实现长期有效减重的关键', '使能量代谢处于负平衡状态'],
    ['老年T2D患者（≥65岁）', '摒弃单纯追求体重数字下降，将功能改善与生活质量作为核心导向'],
    ['重度肥胖合并T2D，其BMI阈值', 'BMI>32.5 kg/m2'],
    ['MDT）全程管理体系覆盖的三个阶段', '减重期、维持期、预防反弹期'],
    ['在Aβ的不同形态中', '可溶性Aβ寡聚体'],
    ['属于阿尔茨海默病（AD）的致病基因', 'APP 基因'],
    ['关于 Aβ 的清除途径', '脑部主要通过蛋白酶降解、血脑屏障清除等途径清除'],
    ['Aβ 的主要清除部位', '脑部'],
    ['阿尔茨海默病（AD）的临床表现三联征', '认知障碍（Cognition）、精神行为症状（BPSD）、日常生活能力受损（ADL）'],
    ['早期筛查中，哪种影像学检查方法', '头部MRI'],
    ['认知障碍自评量表（AD8）的评分标准', '≥2 分'],
    ['血液生物标志物可用于预测脑内Aβ病理改变', '血浆Aβ42/40比值'],
    ['MES量表主要涵盖哪两个认知域', '记忆与执行功能'],
    ['水平变化早于海马萎缩和临床症状', 'CSF Aβ42'],
    ['用于评估患者情景记忆的常用测验', '韦氏记忆量表个人经历分测验'],
    ['MMSE）的缺点，下列说法错误', '不受教育程度影响'],
    ['评估痴呆患者精神行为症状的常用量表', 'NPI'],
    ['主要用于评估痴呆患者的日常生活功能', 'ADL'],
    ['用于评估患者总体退化程度的量表', 'GDS'],
    ['结构影像学检查中，对于痴呆诊断首选', 'MRI'],
    ['路易体痴呆（DLB）的典型运动症状', '帕金森征（行动迟缓，步态异常）'],
    ['属于“精神行为”方面的评估内容', '抑郁和焦虑、幻觉和妄想'],
    ['典型的阿尔茨海默病（AD）代谢模式', '枕顶叶为主，额叶其次'],
    ['关于基因检测的推荐意见', '有明确痴呆家族史的患者应进行基因检测（A级推荐）'],
    ['哪种疾病不属于变性性认知障碍', '血管性痴呆（VaD）'],
    ['阿尔茨海默病的典型病理改变不包括', '路易小体'],
    ['符合变性性认知障碍的特点', '隐匿起病，进行性加重，症状不可逆'],
    ['SWI（磁敏感加权成像）主要用于明确', '微出血'],
    ['关于AD血液生物标志物', '家族性AD患者血浆总Aβ或Aβ42水平增高'],
    ['AD痴呆临床分级的金标准', '临床痴呆评定量表（CDR）'],
    ['2024年IWG-4标准中，将认知功能正常', 'AD高风险人群'],
    ['2014年IWG-2标准', '诊断性标志物和进展性标志物'],
    ['2007年IWG-1标准中', '早期显著情景记忆损伤+至少1项支持性特征'],
    ['AD诊断中生物标志物的应用原则', '生物标志物阳性即可确诊AD，无需结合临床症状'],
    ['AD的特征性萎缩表现，对早发型AD尤为重要', '楔前叶+顶叶萎缩'],
    ['内侧颞叶萎缩评估（MTA）的标准影像学层面', '冠状位 T1WI，脑桥前面水平通过海马体部层面'],
    ['功能性磁共振成像（fMRI）检查中实施学习和回忆任务', '额叶前区和颞叶内侧皮质激活区域缩小，信号强度降低'],
    ['AD患者在磁共振波谱成像（MRS）检查中最早期', 'NAA/Cr 比值降低，MI/Cr 比值升高'],
    ['MRS）中AD最早期的波谱变化', 'NAA/Cr 比值降低，MI/Cr 比值升高'],
    ['关于肝性脑病，下列说法不正确', '前期出现昏迷'],
    ['自身免疫性脑炎（AE）导致的认知障碍', '癫痫、精神障碍'],
    ['血管性认知障碍（VCI）的Newcastle分型', 'VI型指伴AD的脑血管病变'],
    ['不属于“很可能的AD痴呆”核心临床标准', '早期即出现显著的人格改变和行为异常'],
    ['行为变异型额颞叶痴呆，不属于核心临床表现', '找词困难和命名障碍'],
    ['AD临床症状出现前β淀粉样蛋白（Aβ）开始沉积的时间', '10~20年'],
    ['导致血浆AD生物标志物检测存在挑战的因素不包括', '单一样本可检测多项标志物'],
    ['与神经轴索变性和损伤相关的新型体液生物标志物', '神经丝轻链（NfL）'],
    ['脑脊液标本处理中，受检测前因素影响最大的标志物', 'Aβ42'],
    ['相较于PET影像，脑脊液生物标志物的优势不包括', '无创性检测'],
    ['典型AD患者最早出现的临床症状是哪种记忆障碍', '情景记忆障碍'],
    ['AD脑结构变化的典型特点', '早期海马萎缩，晚期全脑萎缩'],
    ['AD临床症状的ABC分类中，A代表', '日常生活能力受损'],
    ['诊断AD源性SCD的核心条件不包括', 'AD生物标志物阴性'],
    ['失读的病变部位主要是', '顶下小叶的缘上回和角回'],
    ['关于 BPSD 的定义描述正确', '认知障碍患者经常出现的紊乱的感知觉、思维内容、心境和行为症状'],
    ['单一出现时，需与颞叶癫痫或颞叶器质性损害鉴别的幻觉类型', '幻嗅'],
    ['在智能障碍基础上出现的与周围环境不协调的愉快体验', '欣快'],
    ['不是 BPSD 造成的不良后果', '减慢痴呆患者认知功能下降速度'],
    ['不属于 AD 患者精神病性症状', '抑郁'],
    ['不属于BPSD中的睡眠障碍类型', '易激惹'],
    ['属于BPSD早期识别量表', '神经精神问卷（NPI）'],
    ['诊治BPSD的价值不包括', '与认知障碍及日常生活能力下降互不影响'],
    ['痴呆临床表现的ABC症状中，A代表', '日常生活能力下降'],
    ['路易体痴呆（DLB）的BPSD核心特点', '幻觉早而明显'],
    ['属于激越中言语攻击性行为', '发出怪声'],
    ['关于BPSD的临床特点，以下表述错误', '精神症状仅在痴呆中晚期出现'],
    ['属于激越中身体非攻击性行为', '不恰当的处理事情'],
    ['美金刚显著预防AD患者以下哪项BPSD症状', '激越/攻击'],
    ['AD患者激越发生率随CDR分期变化的趋势', '逐渐升高'],
    ['AD疾病修饰疗法（DMT）的核心目的', '阻断或延缓疾病的进展'],
    ['用于AD患者睡眠障碍治疗，可增强脑内γ-氨基丁酸与受体结合', '地西泮'],
    ['AD伴发精神行为症状的药物干预核心原则', '抗痴呆治疗为基本，必要时使用精神药物'],
    ['AD药物治疗的核心原则不包括', '根治疾病'],
    ['属于强效高选择性5-羟色胺（5-HT）再摄取抑制剂', '帕罗西汀'],
    ['BPSD临床管理的DICE过程不包括', '药物干预'],
    ['关于BPSD诊治的误区，说法错误', '重视社会心理干预的核心作用'],
    ['经颅直流电刺激（tDCS）的阳极刺激作用', '增强刺激部位神经元的兴奋性'],
    ['不属于AD的脑相关危险因素', '2型糖尿病'],
    ['体育锻炼对AD患者BPSD的影响', '显著改善抑郁症状，整体BPSD无显著改善'],
    ['AD治疗药物的用药安全', '多奈哌齐若出现肝肾功能损害，应考虑减量或停药', 'C'],
    ['共病管理模式的要素不包括', '完全依赖社区志愿者管理', 'E'],
    ['AD与房颤的关系中', '甲状腺功能亢进', 'C'],
    ['AD患者共病的流行病学特点', '65 岁以上老年痴呆患者共病发生率更高', 'D'],
    ['阻塞性睡眠呼吸暂停（OSA）对AD的影响', 'AD患者对CPAP治疗的耐受性极好', 'E']
  ]);
  function verifiedEntry(question) {
    var q = normalize(clean(question).replace(/m²/g, 'm2'));
    var best = null;
    var bestLength = -1;
    for (var i = 0; i < VERIFIED.length; i++) {
      var fragment = normalize(VERIFIED[i][0]);
      if (!fragment || q.indexOf(fragment) < 0) continue;
      if (q === fragment) return VERIFIED[i];
      if (fragment.length > bestLength) {
        best = VERIFIED[i];
        bestLength = fragment.length;
      }
    }
    return best;
  }
  function verifiedAnswer(question) {
    var entry = verifiedEntry(question);
    return entry ? entry[1] : '';
  }
  function verifiedOptionKey(question) {
    var entry = verifiedEntry(question);
    return entry && entry[2] ? clean(entry[2]).toUpperCase() : '';
  }
  function scoreOption(question, option) {
    var score = 0;
    var text = clean(option);
    if (/以上都|以上均/.test(text)) score += 20;
    if (/仅|唯一|完全依赖|快速|严格控制|2小时以上/.test(text)) score -= 8;
    if (/功能改善|生活质量|综合|规范|共识|指南|长期|维持|预防/.test(text)) score += 5;
    if (/不正确|错误|不属于|除外/.test(question)) score = -score;
    score += Math.min(4, text.length / 20);
    return score;
  }
  function parseExam() {
    return Array.from(document.querySelectorAll('table.tablestyle')).map(function (table) {
      var questionNode = table.querySelector('th,thead td,tr:first-child td');
      var question = clean(questionNode ? questionNode.textContent : table.textContent);
      var radios = Array.from(table.querySelectorAll('input[type="radio"]'));
      var options = radios.map(function (input, optionIndex) {
        var row = input.closest('tr,td,label') || input.parentElement;
        var rawText = clean(row ? row.innerText || row.textContent : '');
        var letter = (rawText.match(/^([A-E])[、.．)\s]*/i) || [])[1];
        var text = rawText.replace(/^[A-E][、.．)\s]*/i, '');
        return {
          input: input,
          text: text,
          optionKey: clean(letter || input.value || String(optionIndex + 1)).toUpperCase()
        };
      });
      return { question: question, key: normalize(question).slice(0, 100), options: options };
    }).filter(function (item) { return item.options.length > 1; });
  }
  function optionIsRejected(option, values) {
    values = Array.isArray(values) ? values : [];
    var optionKey = clean(option && option.optionKey).toUpperCase();
    var textKey = normalize(option && option.text);
    return values.some(function (value) {
      var raw = clean(value);
      var tagged = raw.match(/^option:(.+)$/i);
      if (tagged) return clean(tagged[1]).toUpperCase() === optionKey;
      return normalize(raw) === textKey;
    });
  }
  function knownExamOption(item, known, preferredOptionKey, rejectedValues) {
    var knownKey = normalize(known);
    if (!knownKey) return null;
    var matches = item.options.filter(function (option) {
      return normalize(option.text) === knownKey && !optionIsRejected(option, rejectedValues);
    });
    var preferred = clean(preferredOptionKey).toUpperCase();
    if (preferred) {
      var exact = matches.find(function (option) {
        return clean(option.optionKey).toUpperCase() === preferred;
      });
      if (exact) return exact;
    }
    return matches.length === 1 ? matches[0] : null;
  }
  function chooseAnswers(questions, examState) {
    var learned = read(ANSWER_KEY, {});
    var learnedOptions = read(ANSWER_OPTION_KEY, {});
    var attempt = Number(examState.attempt || 0);
    var rejected = examState.rejected || {};
    var divisor = 1;
    return questions.map(function (item) {
      var known = learned[item.key] || verifiedAnswer(item.question);
      var rejectedValues = rejected[item.key] || [];
      var exact = knownExamOption(item, known, learnedOptions[item.key] || verifiedOptionKey(item.question), rejectedValues);
      if (exact) return exact;
      var available = item.options.filter(function (option) {
        return !optionIsRejected(option, rejectedValues);
      });
      if (!available.length) available = item.options.slice();
      var ranked = available.sort(function (a, b) { return scoreOption(item.question, b.text) - scoreOption(item.question, a.text); });
      var choice = ranked[Math.floor(attempt / divisor) % ranked.length];
      divisor *= Math.max(1, ranked.length);
      return choice;
    });
  }
  function fixedAnswerSignature(questions, choices, examState) {
    var learned = read(ANSWER_KEY, {});
    var learnedOptions = read(ANSWER_OPTION_KEY, {});
    var rejected = examState && examState.rejected || {};
    var fixed = questions.every(function (item) {
      var known = learned[item.key] || verifiedAnswer(item.question);
      return !!knownExamOption(
        item, known, learnedOptions[item.key] || verifiedOptionKey(item.question), rejected[item.key] || []
      );
    });
    if (!fixed) return '';
    return choices.map(function (choice, index) {
      return questions[index].key + '=' + normalize(choice && choice.text) +
        '@' + clean(choice && choice.optionKey).toUpperCase();
    }).join('|');
  }
  function answerCombinationCount(questions, examState) {
    var learned = read(ANSWER_KEY, {});
    var learnedOptions = read(ANSWER_OPTION_KEY, {});
    var rejected = examState && examState.rejected || {};
    return questions.reduce(function (total, item) {
      var known = learned[item.key] || verifiedAnswer(item.question);
      var rejectedValues = rejected[item.key] || [];
      var fixed = !!knownExamOption(
        item, known, learnedOptions[item.key] || verifiedOptionKey(item.question), rejectedValues
      );
      if (fixed) return total;
      var availableCount = item.options.filter(function (option) {
        return !optionIsRejected(option, rejectedValues);
      }).length;
      return Math.min(Number.MAX_SAFE_INTEGER, total * Math.max(1, availableCount));
    }, 1);
  }
  function handleExam(retry) {
    retry = Number(retry || 0);
    if (!state.running) return;
    var questions = parseExam();
    if (!questions.length) {
      if (retry < 30) {
        setState({ phase: 'exam', message: '等待考试题目加载（' + (retry + 1) + '/30）' });
        setTimeout(function () { if (route() === 'exam') handleExam(retry + 1); }, 500);
      } else {
        log('[考试] 未识别到题目，停止自动提交');
        setState({ running: false, message: '考试题目加载超时或题型不受支持' });
      }
      return;
    }
    var cwid = queryParam('cwid') || state.currentCwid;
    var exams = read(EXAM_KEY, {});
    var examState = exams[cwid] || { attempt: 0, submitted: {} };
    var combinations = answerCombinationCount(questions, examState);
    var choices = chooseAnswers(questions, examState);
    var fixedSignature = fixedAnswerSignature(questions, choices, examState);
    if (fixedSignature && examState.fixedSignatureAttempted === fixedSignature) {
      setState({ running: false, phase: 'exam', message: '当前已知答案组合已提交过，已停止重复提交' });
      return;
    }
    if (!fixedSignature && Number(examState.attempt || 0) >= combinations) {
      setState({ running: false, phase: 'exam', message: '所有可识别答案组合均已尝试，已停止以避免循环' });
      return;
    }
    if (fixedSignature) {
      examState.fixedSignatureAttempted = fixedSignature;
      exams[cwid] = examState;
      write(EXAM_KEY, exams);
    }
    examState.submittedOptionKeys = examState.submittedOptionKeys || {};
    setState({ phase: 'exam', message: '正在选择 ' + questions.length + ' 道题（第' + (examState.attempt + 1) + '次）' });
    choices.forEach(function (choice, index) {
      setTimeout(function () {
        if (!state.running || !choice || !choice.input) return;
        choice.input.click();
        if (!choice.input.checked) {
          choice.input.checked = true;
          choice.input.dispatchEvent(new Event('change', { bubbles: true }));
        }
        examState.submitted[questions[index].key] = choice.text;
        examState.submittedOptionKeys[questions[index].key] = choice.optionKey;
        exams[cwid] = examState; write(EXAM_KEY, exams);
        if (index === choices.length - 1) {
          setState({ message: '已完成 ' + choices.length + ' 道题，准备提交' });
          setTimeout(function () {
            if (!state.running) return;
            var submit = document.getElementById('btn_submit');
            if (!enabled(submit)) submit = Array.from(document.querySelectorAll('input[type="image"],input[type="submit"],button[type="submit"]')).find(enabled);
            if (submit && enabled(submit)) submit.click();
            else { log('[考试] 未找到唯一提交按钮'); setState({ running: false, message: '提交按钮识别失败' }); }
          }, 3500);
        }
      }, 700 + index * 850);
    });
  }

  function resultCorrect(block, image) {
    var signal = [
      image ? image.src : '', image ? image.alt : '', image ? image.title : '',
      block ? block.className : ''
    ].join(' ');
    if (/wrong|error|cha_img|错误|答错/i.test(signal)) return false;
    if (/bar_img|right|correct|正确|答对/i.test(signal)) return true;
    return null;
  }
  function parseResultAnswers(submitted, submittedOptionKeys) {
    submitted = submitted || {};
    submittedOptionKeys = submittedOptionKeys || {};
    var body = pageText();
    var items = Array.from(document.querySelectorAll('.state_cour_lis'));
    if (items.length) {
      return items.map(function (block) {
        var questionNode = block.querySelector('p[title],p,.q_name,th');
        if (!questionNode) return null;
        var question = clean(questionNode.getAttribute('title') || questionNode.textContent);
        var key = normalize(question).slice(0, 100);
        var image = block.querySelector('img');
        var answerNode = Array.from(block.querySelectorAll('p[title],p,.answer,[class*="answer"]')).find(function (node) {
          return node !== questionNode && /您的答案/.test(clean(node.textContent));
        });
        var rawAnswer = answerNode ? clean(answerNode.getAttribute('title') || answerNode.textContent) : '';
        if (!rawAnswer) {
          var sibling = block.nextElementSibling;
          var local = clean(sibling ? sibling.innerText || sibling.textContent : block.innerText || block.textContent);
          var match = local.match(/您的答案[：:]\s*([^】\n]+)/i);
          rawAnswer = clean(match && match[1]);
        }
        rawAnswer = rawAnswer.replace(/^【?您的答案[：:]\s*/i, '').replace(/】$/, '');
        var optionKey = clean((rawAnswer.match(/^([A-Z])[、.．)\s]*/i) || [])[1] || submittedOptionKeys[key]).toUpperCase();
        var answer = rawAnswer.replace(/^[A-Z][、.．)\s]*/i, '');
        return {
          key: key,
          answer: answer || submitted[key],
          optionKey: optionKey,
          correct: resultCorrect(block, image)
        };
      }).filter(Boolean);
    }
    return body.split(/(?=\d+[、.．])/).map(function (block) {
      var questionMatch = block.match(/^\d+[、.．]\s*([^【\n]+)/);
      if (!questionMatch) return null;
      var key = normalize(questionMatch[1]).slice(0, 100);
      var answerMatch = block.match(/您的答案[：:]\s*([^】\n]+)/i);
      var rawAnswer = clean(answerMatch && answerMatch[1]);
      var optionKey = clean((rawAnswer.match(/^([A-Z])[、.．)\s]*/i) || [])[1] || submittedOptionKeys[key]).toUpperCase();
      var answer = rawAnswer.replace(/^[A-Z][、.．)\s]*/i, '');
      return { key: key, answer: answer || submitted[key], optionKey: optionKey, correct: null };
    }).filter(Boolean);
  }
  function saveLearnedAnswers(records) {
    var learned = read(ANSWER_KEY, {});
    var learnedOptions = read(ANSWER_OPTION_KEY, {});
    var changed = 0;
    records.forEach(function (entry) {
      if (entry.correct !== true || !entry.key || !entry.answer) return;
      var optionKey = clean(entry.optionKey).toUpperCase();
      var answerChanged = learned[entry.key] !== entry.answer;
      var optionChanged = !!optionKey && learnedOptions[entry.key] !== optionKey;
      if (answerChanged) {
        learned[entry.key] = entry.answer;
      }
      if (optionChanged) {
        learnedOptions[entry.key] = optionKey;
      }
      if (answerChanged || optionChanged) {
        changed++;
      }
    });
    if (changed) write(ANSWER_KEY, learned);
    if (changed) write(ANSWER_OPTION_KEY, learnedOptions);
    return changed;
  }
  function saveRejectedAnswers(records, examState) {
    examState.rejected = examState.rejected || {};
    var changed = 0;
    records.forEach(function (entry) {
      if (entry.correct !== false || !entry.key || !entry.answer) return;
      var values = Array.isArray(examState.rejected[entry.key]) ? examState.rejected[entry.key] : [];
      var optionKey = clean(entry.optionKey).toUpperCase();
      var rejectedValue = optionKey ? 'option:' + optionKey : entry.answer;
      var rejectedKey = clean(rejectedValue).toLowerCase();
      if (!values.some(function (value) { return clean(value).toLowerCase() === rejectedKey; })) {
        values.push(rejectedValue);
        changed++;
      }
      examState.rejected[entry.key] = values.slice(-20);
    });
    return changed;
  }
  function savePassedAnswers(submitted, submittedOptionKeys) {
    submittedOptionKeys = submittedOptionKeys || {};
    return saveLearnedAnswers(Object.keys(submitted || {}).map(function (key) {
      return { key: key, answer: submitted[key], optionKey: submittedOptionKeys[key], correct: true };
    }));
  }
  function findResultNextAction() {
    return Array.from(document.querySelectorAll(
      '.state_cour_ul input.state_lis_han,input[value="立即学习"],button,a'
    )).find(function (element) {
      return enabled(element) && /立即学习|下一课|继续学习/.test(actionText(element));
    }) || null;
  }
  function isInteractiveCompletionResult(text, examState) {
    var body = clean(text);
    var submitted = examState && examState.submitted || {};
    return /exam_result_hd\.aspx$/i.test(location.pathname) &&
      !Object.keys(submitted).length &&
      !/考试未通过|考试通过|已通过|考试合格/.test(body);
  }
  function handleResult(retry) {
    retry = Number(retry || 0);
    if (!state.running) return;
    var text = pageText();
    var cwid = queryParam('cwid') || state.currentCwid;
    var exams = read(EXAM_KEY, {});
    var examState = exams[cwid] || { attempt: 0, submitted: {} };
    if (isInteractiveCompletionResult(text, examState)) {
      setState({ phase: 'result', message: '互动病例完成，等待问卷或学分页' });
      if (retry < 40) {
        setTimeout(function () { if (route() === 'result') handleResult(retry + 1); }, 500);
      } else {
        navigate(state.currentCourseUrl || '/pages/study_info_list.aspx');
      }
      return;
    }
    var resultRecords = parseResultAnswers(examState.submitted, examState.submittedOptionKeys);
    var learnedCount = saveLearnedAnswers(resultRecords);
    var rejectedCount = saveRejectedAnswers(resultRecords, examState);
    if (/考试未通过|未通过/.test(text)) {
      if (!resultRecords.length && Object.keys(examState.submitted || {}).length && retry < 20) {
        setState({ phase: 'result', message: '等待考试结果明细加载（' + (retry + 1) + '/20）' });
        setTimeout(function () { if (route() === 'result') handleResult(retry + 1); }, 500);
        return;
      }
      examState.attempt = rejectedCount ? 0 :
        (learnedCount ? 1 : Number(examState.attempt || 0) + 1);
      resultRecords.forEach(function (entry) {
        if (!entry.key || !entry.answer) return;
        examState.submitted[entry.key] = entry.answer;
        examState.submittedOptionKeys = examState.submittedOptionKeys || {};
        if (entry.optionKey) examState.submittedOptionKeys[entry.key] = entry.optionKey;
      });
      exams[cwid] = examState; write(EXAM_KEY, exams);
      setState({
        phase: 'result',
        message: '本次未通过，已学习 ' + learnedCount + ' 题、排除 ' + rejectedCount +
          ' 项，准备第' + (examState.attempt + 1) + '次组合'
      });
      if (!cwid) { setState({ running: false, message: '考试结果缺少 cwid，已停止以避免循环' }); return; }
      setTimeout(function () { if (state.running) navigate('/pages/exam.aspx?cwid=' + encodeURIComponent(cwid)); }, 2500);
      return;
    }
    if (/考试通过|已通过|考试合格|完成项目学习可以申请学分/.test(text)) {
      savePassedAnswers(examState.submitted, examState.submittedOptionKeys);
      delete exams[cwid]; write(EXAM_KEY, exams);
      var nextAction = findResultNextAction();
      setState({ phase: 'course', message: nextAction ? '考试通过，进入下一课件' : '考试通过，返回项目继续' });
      setTimeout(function () {
        if (!state.running) return;
        if (nextAction) humanClick(nextAction);
        else navigate(state.currentCourseUrl || '/pages/study_info_list.aspx');
      }, 1800);
      return;
    }
    if (retry < 20) {
      setState({ phase: 'result', message: '等待考试结果加载（' + (retry + 1) + '/20）' });
      setTimeout(function () { if (route() === 'result') handleResult(retry + 1); }, 500);
    } else {
      setState({ running: false, message: '无法确认考试结果，已停止' });
    }
  }

  function certificateSucceeded() {
    var activeStep = Array.from(document.querySelectorAll('.step_lis.lis_have .step_p,li.active .step_p,li.current .step_p'))
      .map(function (element) { return clean(element.textContent); }).join(' ');
    if (/申请成功/.test(activeStep)) return true;
    var title = clean(document.title);
    var heading = clean((document.querySelector('h1,h2,.success-title,[class*="success"][class*="title"]') || {}).textContent);
    return /申请成功|学分申请成功/.test(title + ' ' + heading) &&
      !/确认申请|证书确认/.test(title + ' ' + heading);
  }

  function findCertificateApplyAction() {
    return Array.from(document.querySelectorAll('button,input[type="button"],input[type="submit"],a')).find(function (element) {
      var text = clean(element.value || element.textContent);
      return enabled(element) && /申请|确认/.test(text) &&
        !/不.*申请|先不申请|取消|撤销|退回|返回/.test(text);
    }) || null;
  }

  function handleCertificate() {
    if (!state.running) return;
    if (certificateSucceeded()) {
      var blocked = Array.isArray(state.blockedApplications) ? state.blockedApplications.slice() : [];
      blocked = blocked.filter(function (url) { return url !== state.currentCourseUrl; });
      setState({
        blockedApplications: blocked,
        blockedApplicationRetryAt: blocked.length ? state.blockedApplicationRetryAt : 0,
        cardRetryQueue: (state.cardRetryQueue || []).filter(function (url) {
          return url !== state.currentCourseUrl;
        }),
        message: '学分申请成功，返回学习记录核验'
      });
      setTimeout(function () { navigate('/pages/study_info_list.aspx'); }, 1000);
      return;
    }
    Array.from(document.querySelectorAll('input[type="radio"]')).forEach(function (input) {
      var group = document.querySelectorAll('input[type="radio"][name="' + CSS.escape(input.name) + '"]');
      if (group.length && !Array.from(group).some(function (item) { return item.checked; })) group[0].click();
    });
    Array.from(document.querySelectorAll('textarea')).forEach(function (area) { if (!area.value) area.value = '满意'; });
    var apply = findCertificateApplyAction();
    if (apply) { setState({ phase: 'certificate', message: '正在申请证书/学分' }); setTimeout(function () { apply.click(); }, 1200); }
    else { setState({ running: false, message: '未找到证书申请按钮' }); }
  }

  function nativeCardSelectionReady() {
    try {
      var selected = Array.isArray(window.selectedCombinations) ? window.selectedCombinations : [];
      var usesBalance = selected.some(function (item) {
        return item && (item.combinationType === 'SCORE' || item.combinationType === 'PERIOD');
      });
      if (usesBalance && typeof window.isScoreEnough === 'function') return Boolean(window.isScoreEnough());
    } catch (_) {}
    return true;
  }
  function usableCardCount(text) {
    var body = clean(text);
    var pattern = /可用培训卡\s*[\(（]?\s*(\d+)\s*[\)）]?/g;
    var match;
    while ((match = pattern.exec(body))) {
      if (match.index === 0 || body.charAt(match.index - 1) !== '不') return Number(match[1]);
    }
    return null;
  }
  function findCardAction() {
    var loading = Array.from(document.querySelectorAll(
      '#loadingOverlay,.loading.show,[class*="loading"][aria-busy="true"]'
    )).find(enabled);
    if (loading) return { type: 'loading', element: loading };
    var text = pageText();
    var availableCount = usableCardCount(text);
    if (availableCount === 0 ||
        (availableCount == null && /这里空空的|暂无.*培训卡/.test(text))) return { type: 'empty' };
    var modalAction = Array.from(document.querySelectorAll(
      '#confirmBtnSecondary,#confirmModalBg button,.confirm-modal button'
    )).find(function (element) {
      return enabled(element) && /用我选的卡|继续使用|确认使用/.test(actionText(element));
    });
    if (modalAction) return { type: 'confirm', element: modalAction };
    var modernSubmit = document.querySelector('#submitBtn');
    var uncheckedCombination = Array.from(document.querySelectorAll(
      '.combination-card:not(.disabled) input[type="checkbox"]:not([disabled]):not(:checked)'
    )).find(enabled);
    if (modernSubmit && enabled(modernSubmit) && nativeCardSelectionReady()) {
      return { type: 'submit', element: modernSubmit };
    }
    if (uncheckedCombination) return { type: 'select', element: uncheckedCombination };
    var radios = Array.from(document.querySelectorAll('input[type="radio"]:not([disabled])')).filter(enabled);
    var selectedRadio = radios.find(function (radio) { return radio.checked; });
    if (!selectedRadio && radios.length) return { type: 'select', element: radios[0] };
    var legacySubmit = findSemanticActions(/确认|确定|立即使用|开始学习|提交/, /取消|返回/, {
      maxText: 18,
      selectors: 'button,input[type="button"],input[type="submit"],a[href],[role="button"]'
    })[0];
    if ((selectedRadio || document.querySelector('input[type="checkbox"]:checked')) && legacySubmit) {
      return { type: 'submit', element: legacySubmit.element };
    }
    return { type: 'attention' };
  }
  function handleCard(retry) {
    retry = Number(retry || 0);
    if (!state.running) return;
    var action = findCardAction();
    if (action.type === 'loading' && retry < 40) {
      setState({ phase: 'card', message: '正在读取可用培训卡' });
      setTimeout(function () { if (state.running && route() === 'card') handleCard(retry + 1); }, 500);
      return;
    }
    if (action.type === 'empty') {
      var blocked = Array.isArray(state.blockedApplications) ? state.blockedApplications.slice() : [];
      if (state.currentCourseUrl && blocked.indexOf(state.currentCourseUrl) < 0) blocked.push(state.currentCourseUrl);
      setState({
        blockedApplications: blocked.slice(-100),
        blockedApplicationYear: Number(policy.year),
        blockedApplicationRetryAt: Date.now() + CARD_RETRY_MS,
        phase: 'study',
        message: '当前课程暂无可用培训卡，先继续其他年度任务'
      });
      setTimeout(function () {
        if (state.running) navigate('/pages/study_info_list.aspx');
      }, 900);
      return;
    }
    if (action.type === 'select') {
      setState({ phase: 'card', message: '正在按站点最优顺序选择培训卡' });
      humanClick(action.element);
      setTimeout(function () { if (state.running && route() === 'card') handleCard(retry + 1); }, 450);
      return;
    }
    if (action.type === 'submit' || action.type === 'confirm') {
      setState({
        phase: 'card',
        message: action.type === 'confirm' ? '正在确认使用站点已选培训卡' : '已按站点最优顺序选卡，正在提交'
      });
      setTimeout(function () {
        if (!state.running || route() !== 'card') return;
        humanClick(action.element);
        setTimeout(function () {
          if (state.running && route() === 'card') handleCard(retry + 1);
        }, 900);
      }, 650);
      return;
    }
    setState({
      running: false,
      paused: true,
      phase: 'card',
      message: '培训卡页面需要确认，请处理后点击开始/继续'
    });
  }

  function setInputValue(input, value) {
    if (!input) return;
    var prototype = input instanceof HTMLTextAreaElement ? HTMLTextAreaElement.prototype : HTMLInputElement.prototype;
    var descriptor = Object.getOwnPropertyDescriptor(prototype, 'value');
    try {
      if (descriptor && descriptor.set) descriptor.set.call(input, value);
      else input.value = value;
    } catch (_) { input.value = value; }
    input.dispatchEvent(new Event('input', { bubbles: true }));
    input.dispatchEvent(new Event('change', { bubbles: true }));
  }

  function loginElements() {
    function firstVisible(selector) {
      var nodes = Array.from(document.querySelectorAll(selector));
      return nodes.find(visible) || null;
    }
    function firstAny(selector) {
      return document.querySelector(selector);
    }
    return {
      more: firstVisible('#show_type_more'),
      passwordMode: firstVisible('#type_pwd'),
      username: firstVisible('#txt_user_name,input[name="txt_user_name"],input[placeholder*="用户名"],input[placeholder*="身份证"]'),
      password: firstVisible('#txt_user_pwd,input[placeholder*="密码"]'),
      passwordReal: firstAny('#txt_user_pwd_real,input[name="txt_user_pwd"][type="hidden"]'),
      captcha: firstVisible('#txt_img_code,input[name="txt_img_code"],input[placeholder*="图形验证码"]'),
      captchaImage: firstVisible('#yzm_img,img[src*="CheckCode"],img[src*="captcha" i]'),
      agreement: firstVisible('#agree1,input[type="checkbox"][name*="agree" i]'),
      submit: firstVisible('.btn_login,#btn_login,input[type="submit"],button[type="submit"]')
    };
  }

  function captchaSettings() {
    var stored = read(CAPTCHA_CONFIG_KEY, {});
    var bridged = '';
    try { bridged = clean(pageWindow.__HY8_CAPTCHA_PROVIDER_URL); } catch (_) {}
    return {
      providerUrl: bridged || clean(stored.providerUrl) || 'http://127.0.0.1:17891/solve',
      expectedLength: Math.max(1, Number(stored.expectedLength || 5)),
      maxAttempts: Math.max(1, Number(stored.maxAttempts || 6))
    };
  }

  function captchaImageBlob(image) {
    return new Promise(function (resolve, reject) {
      try {
        if (!image || !image.complete || !image.naturalWidth || !image.naturalHeight) {
          reject(new Error('验证码图片尚在加载'));
          return;
        }
        var canvas = document.createElement('canvas');
        canvas.width = image.naturalWidth;
        canvas.height = image.naturalHeight;
        canvas.getContext('2d').drawImage(image, 0, 0);
        canvas.toBlob(function (blob) {
          if (blob) resolve(blob);
          else reject(new Error('验证码图片转换结果为空'));
        }, 'image/png');
      } catch (error) { reject(error); }
    });
  }

  function requestCaptchaSolution(providerUrl, imageBlob) {
    if (typeof GM_xmlhttpRequest === 'function') {
      return new Promise(function (resolve, reject) {
        GM_xmlhttpRequest({
          method: 'POST',
          url: providerUrl,
          data: imageBlob,
          headers: { 'Content-Type': 'image/png' },
          responseType: 'json',
          timeout: 60000,
          onload: function (response) {
            var payload = response.response;
            if (!payload && response.responseText) {
              try { payload = JSON.parse(response.responseText); } catch (_) {}
            }
            if (response.status >= 200 && response.status < 300 && payload && payload.ok) resolve(payload);
            else reject(new Error(payload && payload.error || '本机验证码服务返回异常'));
          },
          onerror: function () { reject(new Error('本机验证码服务连接失败')); },
          ontimeout: function () { reject(new Error('本机验证码服务响应超时')); }
        });
      });
    }
    return fetch(providerUrl, {
      method: 'POST',
      headers: { 'Content-Type': 'image/png' },
      body: imageBlob
    }).then(function (response) {
      return response.json().then(function (payload) {
        if (!response.ok || !payload.ok) throw new Error(payload.error || '本机验证码服务返回异常');
        return payload;
      });
    });
  }

  function resetLoginForRetry(elements, delay) {
    var dialog = document.querySelector(
      '.layui-layer-btn0,.layui-layer-close,.ui-dialog-button button,.el-message-box__btns button'
    );
    if (dialog && enabled(dialog)) humanClick(dialog);
    if (elements.captcha) setInputValue(elements.captcha, '');
    if (elements.captchaImage) humanClick(elements.captchaImage);
    window.__HY8_CAPTCHA_BUSY = false;
    setTimeout(function () { if (route() === 'login' && state.running) handleLogin(); }, Number(delay || 900));
  }

  function solveLoginCaptcha(elements) {
    if (window.__HY8_CAPTCHA_BUSY) return;
    var settings = captchaSettings();
    var attempts = Number(sessionStorage.getItem('HY8_CAPTCHA_ATTEMPTS') || 0);
    if (attempts >= settings.maxAttempts) {
      sessionStorage.setItem('HY8_CAPTCHA_ATTEMPTS', '0');
      setState({ phase: 'login', message: '验证码已完成一轮识别，30 秒后自动开始新一轮' });
      setTimeout(function () {
        if (route() === 'login' && state.running) resetLoginForRetry(loginElements(), 500);
      }, 30000);
      return;
    }
    window.__HY8_CAPTCHA_BUSY = true;
    setState({
      phase: 'login',
      message: '正在本机识别图形验证码（第 ' + (attempts + 1) + '/' + settings.maxAttempts + ' 次）'
    });
    captchaImageBlob(elements.captchaImage)
      .then(function (blob) { return requestCaptchaSolution(settings.providerUrl, blob); })
      .then(function (result) {
        var code = clean(result.code).replace(/\D/g, '');
        if (code.length !== settings.expectedLength) throw new Error('验证码识别位数不匹配');
        sessionStorage.setItem('HY8_CAPTCHA_ATTEMPTS', String(attempts + 1));
        setInputValue(elements.captcha, code);
        window.__HY8_CAPTCHA_BUSY = false;
        submitLoginWhenReady(loginElements());
      })
      .catch(function (error) {
        window.__HY8_CAPTCHA_BUSY = false;
        setState({ phase: 'login', message: '本机验证码识别重试：' + error.message });
        setTimeout(function () {
          if (route() === 'login' && state.running) solveLoginCaptcha(loginElements());
        }, 5000);
      });
  }

  function submitLoginWhenReady(elements) {
    if (!state.running || route() !== 'login') return;
    var codeRequired = elements.captcha && visible(elements.captcha) && elements.captchaImage && visible(elements.captchaImage);
    if (codeRequired && !clean(elements.captcha.value)) {
      solveLoginCaptcha(elements);
      return;
    }
    if (!elements.submit || !enabled(elements.submit)) {
      setState({ phase: 'login', message: '登录信息已填写，正在等待登录按钮可用' });
      setTimeout(function () { submitLoginWhenReady(loginElements()); }, 500);
      return;
    }
    if (window.__HY8_LOGIN_SUBMITTING) return;
    window.__HY8_LOGIN_SUBMITTING = true;
    setState({ phase: 'login', message: '正在登录，成功后自动恢复年度任务' });
    setTimeout(function () {
      if (route() !== 'login' || !state.running) return;
      humanClick(elements.submit);
      setTimeout(function () {
        window.__HY8_LOGIN_SUBMITTING = false;
        if (route() === 'login' && state.running) {
          setState({ phase: 'login', message: '页面校验未通过，正在刷新验证码重试' });
          resetLoginForRetry(loginElements(), 900);
        } else {
          try { sessionStorage.removeItem('HY8_CAPTCHA_ATTEMPTS'); } catch (_) {}
        }
      }, 2500);
    }, 500);
  }

  function handleLogin() {
    var credentials = read(LOGIN_KEY, {});
    var elements = loginElements();
    if (!credentials.username || !credentials.password) {
      setState({ phase: 'login', message: '请登录；也可从 Tampermonkey 菜单设置本机自动登录信息' });
      return;
    }
    if (!elements.username || !elements.password) {
      if (elements.more && enabled(elements.more)) {
        setState({ phase: 'login', message: '正在切换到账号登录' });
        humanClick(elements.more);
      } else if (elements.passwordMode && enabled(elements.passwordMode)) {
        setState({ phase: 'login', message: '正在切换到密码登录' });
        humanClick(elements.passwordMode);
      } else {
        setState({ phase: 'login', message: '等待登录表单加载' });
      }
      setTimeout(function () { if (route() === 'login') handleLogin(); }, 500);
      return;
    }
    setInputValue(elements.username, credentials.username);
    setInputValue(elements.password, credentials.password);
    if (elements.passwordReal) setInputValue(elements.passwordReal, credentials.password);
    if (elements.agreement && !elements.agreement.checked) humanClick(elements.agreement);
    submitLoginWhenReady(elements);
  }

  function examCaptchaElements() {
    var image = document.querySelector('#imgCheckCode,img.yzm_img,img[src*="CheckCode"]');
    var input = document.querySelector('#txtCheckCode,input[name="txtCheckCode"],input[name*="code" i],input[id*="code" i]');
    var submit = document.querySelector('#btnYes,input[type="submit"],button[type="submit"]');
    return { image: image, input: input, submit: submit };
  }

  function solveExamCaptcha(elements) {
    if (window.__HY8_EXAM_CAPTCHA_BUSY) return;
    var settings = captchaSettings();
    var attempts = Number(sessionStorage.getItem('HY8_EXAM_CAPTCHA_ATTEMPTS') || 0);
    if (attempts >= settings.maxAttempts) {
      sessionStorage.setItem('HY8_EXAM_CAPTCHA_ATTEMPTS', '0');
      setState({ phase: 'captcha', message: '考试验证码已完成一轮识别，30 秒后自动开始新一轮' });
      setTimeout(function () {
        if (route() !== 'captcha' || !state.running) return;
        var current = examCaptchaElements();
        if (current.input) setInputValue(current.input, '');
        if (current.image) humanClick(current.image);
        solveExamCaptcha(current);
      }, 30000);
      return;
    }
    if (!elements.image || !elements.input || !elements.submit) {
      setState({ phase: 'captcha', message: '等待考试验证码页面加载' });
      setTimeout(function () { if (route() === 'captcha' && state.running) handleCaptcha(); }, 500);
      return;
    }
    if (!elements.image.complete || !elements.image.naturalWidth) {
      humanClick(elements.image);
      setState({ phase: 'captcha', message: '正在刷新考试验证码图片' });
      setTimeout(function () { if (route() === 'captcha' && state.running) handleCaptcha(); }, 600);
      return;
    }
    window.__HY8_EXAM_CAPTCHA_BUSY = true;
    setState({
      phase: 'captcha',
      message: '正在本机识别考试验证码（第 ' + (attempts + 1) + '/' + settings.maxAttempts + ' 次）'
    });
    captchaImageBlob(elements.image)
      .then(function (blob) { return requestCaptchaSolution(settings.providerUrl, blob); })
      .then(function (result) {
        var code = clean(result.code).replace(/\D/g, '');
        if (code.length !== settings.expectedLength) throw new Error('考试验证码识别位数不匹配');
        sessionStorage.setItem('HY8_EXAM_CAPTCHA_ATTEMPTS', String(attempts + 1));
        setInputValue(elements.input, code);
        setState({ phase: 'captcha', message: '考试验证码识别完成，正在提交' });
        humanClick(elements.submit);
        setTimeout(function () {
          window.__HY8_EXAM_CAPTCHA_BUSY = false;
          if (route() === 'captcha' && state.running) {
            var current = examCaptchaElements();
            if (current.input) setInputValue(current.input, '');
            if (current.image) humanClick(current.image);
            setTimeout(handleCaptcha, 600);
          } else {
            try { sessionStorage.removeItem('HY8_EXAM_CAPTCHA_ATTEMPTS'); } catch (_) {}
          }
        }, 2500);
      })
      .catch(function (error) {
        window.__HY8_EXAM_CAPTCHA_BUSY = false;
        setState({ phase: 'captcha', message: '考试验证码本机识别重试：' + error.message });
        setTimeout(function () { if (route() === 'captcha' && state.running) handleCaptcha(); }, 5000);
      });
  }

  function handleCaptcha() {
    if (!state.running) return;
    solveExamCaptcha(examCaptchaElements());
  }

  function selectSurveyChoice(input) {
    if (!input) return;
    var surrogate = input.parentElement && input.parentElement.querySelector('a.jqradio,a.jqcheck');
    if (surrogate && enabled(surrogate)) humanClick(surrogate);
    else input.click();
    if (!input.checked) {
      input.checked = true;
      input.dispatchEvent(new Event('input', { bubbles: true }));
      input.dispatchEvent(new Event('change', { bubbles: true }));
    }
  }

  function handleSurvey() {
    if (!state.running) return;
    var form = document.querySelector('form') || document.body;
    var securityPopup = document.querySelector('#aliyunCaptcha-window-popup');
    if (securityPopup && enabled(securityPopup) &&
        /点击开始智能验证|请完成安全验证/.test(clean(securityPopup.textContent))) {
      setState({ phase: 'survey', message: '等待浏览器完成问卷安全验证' });
      return;
    }
    var resume = Array.from(document.querySelectorAll(
      '.layui-layer-btn0,.ui-dialog-button button,.el-message-box__btns button,[role="dialog"] button'
    )).find(function (element) {
      return enabled(element) && /确认|继续|恢复/.test(clean(element.value || element.textContent));
    });
    if (resume && /需要安全校验|重新提交/.test(pageText())) {
      setState({ phase: 'survey', message: '确认问卷安全校验并重新提交' });
      humanClick(resume);
      setTimeout(function () {
        if (route() === 'survey' && state.running) handleSurvey();
      }, 700);
      return;
    }
    if (resume && /之前已经回答|继续上次|恢复.*答/.test(pageText())) {
      setState({ phase: 'survey', message: '正在恢复已保存的问卷进度' });
      humanClick(resume);
      setTimeout(function () {
        if (route() === 'survey' && state.running) handleSurvey();
      }, 700);
      return;
    }
    var groups = {};
    Array.from(form.querySelectorAll('input[type="radio"]')).forEach(function (input) { (groups[input.name] || (groups[input.name] = [])).push(input); });
    Object.keys(groups).forEach(function (name) {
      if (!groups[name].some(function (item) { return item.checked; })) selectSurveyChoice(groups[name][0]);
    });
    var checkboxGroups = {};
    Array.from(form.querySelectorAll('input[type="checkbox"]')).forEach(function (input) {
      var name = input.name || input.id || 'anonymous';
      (checkboxGroups[name] || (checkboxGroups[name] = [])).push(input);
    });
    Object.keys(checkboxGroups).forEach(function (name) {
      var choices = checkboxGroups[name];
      if (!choices.some(function (item) { return item.checked; })) {
        var choice = choices.find(function (item) {
          var label = item.closest('label') || item.parentElement;
          return !/其他|无|以上都不是/.test(clean(label && label.textContent));
        }) || choices[0];
        selectSurveyChoice(choice);
      }
    });
    Array.from(form.querySelectorAll(
      '.field[type="11"],[data-type="11"],[data-question-type="11"]'
    )).forEach(function (ranking) {
      var options = Array.from(ranking.querySelectorAll('li,[data-value],[class*="sort-item"]')).filter(enabled);
      var selected = options.filter(function (item) {
        return /(^|\s)(check|checked|selected|active)(\s|$)/i.test(classText(item)) ||
          clean((item.querySelector('.sortnum,[class*="sort-num"]') || {}).textContent);
      });
      if (!selected.length) {
        options.filter(function (item) {
          return !/其他|无|以上都不是/.test(actionText(item));
        }).forEach(humanClick);
      }
    });
    Array.from(form.querySelectorAll(
      'input[type="text"][name],input[type="number"][name],input[type="range"][name]'
    )).forEach(function (input) {
      if (clean(input.value)) return;
      var minimum = Number(input.min);
      var maximum = Number(input.max);
      var bounded = isFinite(minimum) && isFinite(maximum) && maximum > minimum;
      setInputValue(input, bounded ? String(Math.round((minimum + maximum) / 2)) : '满意');
    });
    Array.from(form.querySelectorAll('select')).forEach(function (select) {
      if (!select.value && select.options.length > 1) {
        select.selectedIndex = 1;
        select.dispatchEvent(new Event('change', { bubbles: true }));
      }
    });
    Array.from(form.querySelectorAll('textarea')).forEach(function (area) { if (!area.value) setInputValue(area, '无'); });
    var submit = Array.from(form.querySelectorAll(
      '#ctlNext,.submitbtn,button,input[type="submit"],input[type="button"],[role="button"]'
    )).find(function (element) {
      return enabled(element) && /提交|完成|下一步/.test(clean(element.value || element.textContent));
    });
    if (submit) {
      setState({ phase: 'survey', message: '问卷必填项已完成，正在提交' });
      setTimeout(function () { if (state.running) humanClick(submit); }, 1200);
    } else {
      setState({ phase: 'survey', message: '问卷已填写，等待提交入口加载' });
      setTimeout(function () {
        if (route() === 'survey' && state.running) handleSurvey();
      }, 1000);
    }
  }

  function findCaseAction() {
    var quiz = parseCaseQuestion();
    var quizAction = findCaseQuestionAction(quiz);
    if (quizAction) return quizAction;
    var video = caseVideoStatus();
    if (video.active && !video.done) {
      if (video.media && video.paused) {
        var directPlay = Array.from(document.querySelectorAll(
          '.pv-cover .pv-icon-btn-play,.pv-playpause.pv-icon-btn-play,.xgplayer-start,' +
          '[class*="video"] [class*="play"],video'
        )).find(function (element) {
          return element === video.media || enabled(element);
        });
        if (directPlay) return { element: directPlay, text: '播放病例视频', kind: 'case-video-play' };
      }
      var play = findSemanticActions(/^(播放|继续播放|开始播放)$/, /倍速|快进|全屏|音量|清晰|设置/, {
        maxText: 12,
        score: function (element) {
          return /play|start|pause|pv-icon-btn-play|xgplayer-start/i.test(classText(element)) ? 35 : 0;
        }
      })[0];
      return play || null;
    }
    var allow = /^(查看病例|开始学习|开始|继续学习|继续|下一步|下一页|进入|确认|提交|完成学习|完成)$/;
    var deny = /返回|退出|关闭|取消|上一页|上一步|暂停|重置|分享|收藏/;
    var candidates = findSemanticActions(allow, deny, {
      maxText: 18,
      score: function (element, text) {
        var extra = 0;
        if (/病例|学习|下一|继续|提交|完成/.test(text)) extra += 40;
        if (/btn|button|view|next|submit|case|start|continue|primary|footer/i.test(classText(element))) extra += 20;
        return extra;
      }
    });
    return candidates[0] || null;
  }

  function parseCaseQuestion() {
    var questionNode = document.querySelector(
      '.question-text,[class*="question-text"],[class*="question-title-text"],[data-question]'
    );
    var optionNodes = Array.from(document.querySelectorAll(
      '.option-content,[class*="question-option"],[class*="option-item"],[class*="answer-option"]'
    )).filter(function (element, index, all) {
      if (!enabled(element)) return false;
      var optionText = clean(element.innerText || element.textContent);
      if (!optionText || !/^[A-Z][、.．)\s]/i.test(optionText)) return false;
      return all.findIndex(function (other) {
        return normalize(other.innerText || other.textContent) === normalize(optionText);
      }) === index;
    });
    if (!questionNode || optionNodes.length < 2) return null;
    var question = clean(questionNode.innerText || questionNode.textContent);
    var options = optionNodes.map(function (element, optionIndex) {
      var raw = clean(element.innerText || element.textContent);
      var optionKey = clean((raw.match(/^([A-Z])[、.．)\s]*/i) || [])[1] ||
        String.fromCharCode(65 + optionIndex)).toUpperCase();
      return {
        element: element,
        optionKey: optionKey,
        text: raw.replace(/^[A-Z][、.．)\s]*/i, '')
      };
    });
    var text = pageText();
    var reference = clean((text.match(/参考答案\s*[：:]\s*([A-Z])/i) || [])[1]).toUpperCase();
    return {
      question: question,
      key: normalize(question).slice(0, 100),
      options: options,
      result: !!reference || /(?:回答)?正确[!！]?|(?:回答)?错误[!！]?/.test(text),
      referenceOptionKey: reference,
      submit: Array.from(document.querySelectorAll(
        '.problem-page-right,.click-active,button,input[type="submit"],input[type="button"],[role="button"]'
      )).find(function (element) {
        return enabled(element) && /^提交$/.test(actionText(element));
      }) || null
    };
  }

  function caseOptionSelected(option) {
    if (!option || !option.element) return false;
    var element = option.element;
    var nodes = [element, element.parentElement, element.querySelector('input')].filter(Boolean);
    return nodes.some(function (node) {
      var classes = classText(node);
      return node.checked === true || node.getAttribute('aria-checked') === 'true' ||
        /(^|\s)(?:active|selected|checked|is-checked|option-active)(?:\s|$)/i.test(classes);
    });
  }

  function chooseCaseQuestionOption(quiz) {
    if (!quiz || !quiz.options.length) return null;
    var learned = read(ANSWER_KEY, {});
    var learnedOptions = read(ANSWER_OPTION_KEY, {});
    var choice = knownExamOption(
      quiz,
      learned[quiz.key] || verifiedAnswer(quiz.question),
      learnedOptions[quiz.key] || verifiedOptionKey(quiz.question),
      []
    );
    if (choice) return choice;
    var matchingAnswers = quiz.options.filter(function (option) {
      return VERIFIED.some(function (entry) {
        var answer = normalize(entry[1]);
        var optionText = normalize(option.text);
        return answer.length >= 6 && (optionText.indexOf(answer) >= 0 || answer.indexOf(optionText) >= 0);
      });
    });
    if (matchingAnswers.length === 1) return matchingAnswers[0];
    return quiz.options.slice().sort(function (a, b) {
      return scoreOption(quiz.question, b.text) - scoreOption(quiz.question, a.text);
    })[0];
  }

  function learnCaseQuestionResult(quiz) {
    if (!quiz || !quiz.result || !quiz.referenceOptionKey) return 0;
    var correct = quiz.options.find(function (option) {
      return option.optionKey === quiz.referenceOptionKey;
    });
    if (!correct) return 0;
    return saveLearnedAnswers([{
      key: quiz.key,
      answer: correct.text,
      optionKey: correct.optionKey,
      correct: true
    }]);
  }

  function findCaseQuestionAction(quiz) {
    if (!quiz) return null;
    if (quiz.result) {
      learnCaseQuestionResult(quiz);
      window.__HY8_CASE_SELECTION = null;
      return null;
    }
    var selected = quiz.options.find(caseOptionSelected);
    var memory = window.__HY8_CASE_SELECTION;
    if (memory && memory.key !== quiz.key) memory = null;
    if (selected || memory) {
      if (quiz.submit) return { element: quiz.submit, text: '提交', kind: 'case-submit', quizKey: quiz.key };
      return null;
    }
    var choice = chooseCaseQuestionOption(quiz);
    if (!choice) return null;
    return {
      element: choice.element,
      text: '选择答案：' + choice.optionKey,
      kind: 'case-option',
      quizKey: quiz.key,
      optionKey: choice.optionKey,
      optionText: choice.text
    };
  }

  function handleCase() {
    if (!state.running) return;
    var lastActionElement = null;
    var lastActionAt = 0;
    if (window.__HY8_TIMER) clearInterval(window.__HY8_TIMER);
    window.__HY8_TIMER = setInterval(function () {
      if (!state.running) { clearInterval(window.__HY8_TIMER); return; }
      var action = findCaseAction();
      if (action) {
        if (action.element !== lastActionElement || Date.now() - lastActionAt >= 3500) {
          lastActionElement = action.element;
          lastActionAt = Date.now();
          log('[病例] 点击：' + action.text);
          humanClick(action.element);
          if (action.kind === 'case-option') {
            window.__HY8_CASE_SELECTION = {
              key: action.quizKey,
              optionKey: action.optionKey,
              optionText: action.optionText,
              at: Date.now()
            };
          }
          if (action.kind === 'case-video-play') {
            var media = caseVideoStatus().media;
            if (media && media.paused && typeof media.play === 'function') {
              try {
                var playResult = media.play();
                if (playResult && typeof playResult.catch === 'function') playResult.catch(function () {});
              } catch (_) {}
            }
          }
        }
      } else {
        var video = caseVideoStatus();
        if (video.active && !video.done) setState({ phase: 'case', message: '病例视频观看中，剩余约 ' + Math.ceil(video.remaining / 60) + ' 分钟' });
      }
      var text = pageText();
      if (/学习完毕|病例完成/.test(text)) { clearInterval(window.__HY8_TIMER); navigate(state.currentCourseUrl || '/pages/study_info_list.aspx'); }
    }, 1300);
  }

  function catalogSource() {
    var text = clean((document.querySelector('h1,h2,.title,.nav .active,.tab.active') || {}).textContent || '');
    if (/\/fme/i.test(location.pathname) || /全员|专项/.test(text)) return '全员专项';
    if (/公需|公共必修|公共科目/.test(text) || /[?&](?:type|category)=public/i.test(location.search)) return '继续教育公需课';
    return '继续教育';
  }

  function scanCatalog() {
    var links = uniqueElements(Array.from(document.querySelectorAll(
      'a[href*="course.aspx?cid="],a[href*="/course/"][data-id],.jet_lis a[href*="cid="],[class*="course-card"] a[href]'
    )));
    var source = catalogSource();
    return links.map(function (link) {
      var card = link.closest('.jet_lis,li,[class*="course-card"],[class*="course-item"],[class*="project-item"]') || link.parentElement;
      var text = clean(card ? card.innerText || card.textContent : link.textContent);
      var url = absolute(link.href || link.getAttribute('data-href') || link.getAttribute('href'));
      return Core.normalizeRecord({
        id: queryParam('cid', url) || link.getAttribute('data-id') || url,
        name: clean(link.getAttribute('title') || link.textContent) || text.slice(0, 80),
        url: url,
        year: Core.parseYear(text, policy.year),
        creditText: text,
        durationText: text,
        category: source === '继续教育公需课' ? 'public' : Core.detectCategory(text),
        source: source === '继续教育' ? Core.detectSource(text) : source,
        statusText: text,
        enrolled: false,
        text: text
      }, policy);
    }).filter(function (record, index, all) {
      return record.year === Number(policy.year) && !/^(applied|completed)$/.test(record.status) &&
        all.findIndex(function (other) { return other.id === record.id; }) === index;
    });
  }

  function nextCatalogDestination(plan, visitedSources) {
    var candidates = [];
    if (plan.projectedPublic < Number(policy.publicTarget)) {
      candidates.push({
        source: '继续教育公需课',
        pattern: /公需|公共必修|公共科目/,
        url: findCatalogLink(/公需|公共必修|公共科目/, '/cme/index.html')
      });
    } else {
      candidates.push({
        source: '继续教育',
        pattern: /继续教育|继教|专业科目/,
        url: findCatalogLink(/继续教育|继教|专业科目/, '/cme/index.html')
      });
      candidates.push({
        source: '全员专项',
        pattern: /全员|专项/,
        url: findCatalogLink(/全员|专项/, '/pages/fme.aspx')
      });
    }
    return candidates.find(function (candidate) { return visitedSources.indexOf(candidate.source) < 0; }) || null;
  }

  function handleCatalog(retry) {
    if (!state.running) return;
    retry = Number(retry || 0);
    var currentSource = catalogSource();
    var scanned = scanCatalog();
    if (!scanned.length && retry < 30) {
      setState({ phase: 'catalog', message: '等待' + currentSource + '目录加载（' + (retry + 1) + '/30）' });
      setTimeout(function () { if (route() === 'catalog') handleCatalog(retry + 1); }, 500);
      return;
    }
    var catalog = mergeRecords(state.catalogRecords || [], scanned);
    var plan = Core.buildAnnualPlan(state.studyRecords || [], catalog, policy);
    var visited = state.catalogYear === Number(policy.year) && Array.isArray(state.catalogVisited) ? state.catalogVisited : [];
    var sources = Array.isArray(state.catalogSourcesVisited) ? state.catalogSourcesVisited.slice() : [];
    if (sources.indexOf(currentSource) < 0) sources.push(currentSource);
    setState({
      catalogYear: Number(policy.year),
      catalogRecords: catalog,
      catalogSourcesVisited: sources,
      planTasks: simplifyTasks(plan.tasks),
      publicProjected: plan.projectedPublic,
      otherProjected: plan.projectedOther,
      phase: 'catalog',
      message: currentSource + '已扫描 ' + scanned.length + ' 门；计划公需 ' +
        plan.projectedPublic + '，其他 ' + plan.projectedOther
    });
    var enrollment = plan.tasks.find(function (task) {
      return task.type === 'enroll' && task.record.url && visited.indexOf(task.record.url) < 0;
    });
    if (enrollment) {
      setState({
        currentCourseUrl: enrollment.record.url,
        currentCourseName: enrollment.record.name,
        catalogVisited: visited.concat(enrollment.record.url).slice(-200),
        phase: 'course',
        message: enrollment.reason + '：' + enrollment.record.name
      });
      navigate(enrollment.record.url);
      return;
    }
    var unknown = scanned.find(function (record) {
      return record.credit <= 0 && record.url && visited.indexOf(record.url) < 0;
    });
    if (unknown) {
      setState({
        currentCourseUrl: unknown.url,
        currentCourseName: unknown.name,
        catalogVisited: visited.concat(unknown.url).slice(-200),
        phase: 'course',
        message: '读取课程学分：' + unknown.name
      });
      navigate(unknown.url);
      return;
    }
    var next = nextCatalogDestination(plan, sources);
    if (next) {
      setState({ message: '切换到' + next.source + '继续补全候选课程' });
      if (absolute(next.url) === location.href) {
        var tab = findSemanticActions(next.pattern, /返回|退出|取消/, {
          maxText: 20,
          selectors: 'a[href],button,[role="button"],[class*="tab"],[data-type],[data-category]'
        })[0];
        if (tab) {
          setState({ catalogSourcesVisited: sources.concat(next.source) });
          humanClick(tab.element);
          setTimeout(function () { if (route() === 'catalog' && state.running) handleCatalog(); }, 900);
        } else {
          setState({
            catalogSourcesVisited: sources.concat(next.source),
            running: false,
            paused: true,
            message: '未找到' + next.source + '目录入口；已保留当前规划'
          });
        }
      } else navigate(next.url);
      return;
    }
    if (plan.shortages.public <= 0 && plan.shortages.other <= 0) {
      var cached = plan.tasks.find(function (task) { return task.type === 'enroll' && task.record.url; });
      if (cached) { navigate(cached.record.url); return; }
    }
    setState({
      running: false,
      paused: true,
      message: '目录扫描完成；仍缺公需 ' + plan.shortages.public + ' 分、其他 ' + plan.shortages.other + ' 分'
    });
  }

  function runRoute(force) {
    var current = route();
    state.lastRoute = current; saveState();
    if (current !== 'player') createUI();
    if (!state.running && !force) { render(); return; }
    if (current === 'study') handleStudy();
    else if (current === 'course') handleCourse();
    else if (current === 'player') handlePlayer();
    else if (current === 'exam') handleExam();
    else if (current === 'result') handleResult();
    else if (current === 'certificate') handleCertificate();
    else if (current === 'card') handleCard();
    else if (current === 'login') handleLogin();
    else if (current === 'captcha') handleCaptcha();
    else if (current === 'survey') handleSurvey();
    else if (current === 'case') handleCase();
    else if (current === 'catalog') handleCatalog();
  }

  function init() { registerMenus(); createUI(); runRoute(false); }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', function () { setTimeout(init, 350); }, { once: true });
  else setTimeout(init, 0);

  if (window.__HY_TEST_MODE__) {
    window.__HY8_TEST_API__ = {
      route: route, scanStudy: scanStudy, scanCoursewares: scanCoursewares,
      scanCatalog: scanCatalog, scanCourseDetail: scanCourseDetail, loginElements: loginElements,
      selectStudyYear: selectStudyYear,
      parseExam: parseExam, parseResultAnswers: parseResultAnswers, saveLearnedAnswers: saveLearnedAnswers,
      saveRejectedAnswers: saveRejectedAnswers,
      findResultNextAction: findResultNextAction, isInteractiveCompletionResult: isInteractiveCompletionResult,
      verifiedAnswer: verifiedAnswer, verifiedOptionKey: verifiedOptionKey,
      scoreOption: scoreOption, chooseAnswers: chooseAnswers, answerCombinationCount: answerCombinationCount,
      fixedAnswerSignature: fixedAnswerSignature, optionIsRejected: optionIsRejected, knownExamOption: knownExamOption,
      enabled: enabled, normalize: normalize, findCaseAction: findCaseAction, caseVideoStatus: caseVideoStatus,
      parseCaseQuestion: parseCaseQuestion, chooseCaseQuestionOption: chooseCaseQuestionOption,
      findCaseQuestionAction: findCaseQuestionAction, learnCaseQuestionResult: learnCaseQuestionResult,
      playerMediaStatus: playerMediaStatus, findPlayerPrompt: findPlayerPrompt,
      needsTrustedPlayerClick: needsTrustedPlayerClick,
      parseClassroomQuiz: parseClassroomQuiz, chooseClassroomOption: chooseClassroomOption
      , captchaSettings: captchaSettings, requestCaptchaSolution: requestCaptchaSolution
      , certificateSucceeded: certificateSucceeded, findCertificateApplyAction: findCertificateApplyAction
      , examCaptchaElements: examCaptchaElements, solveExamCaptcha: solveExamCaptcha
      , selectSurveyChoice: selectSurveyChoice, handleSurvey: handleSurvey
      , blockedApplicationDecision: blockedApplicationDecision
      , usableCardCount: usableCardCount, nativeCardSelectionReady: nativeCardSelectionReady
      , findCardAction: findCardAction, handleCard: handleCard
      , savePassedAnswers: savePassedAnswers
    };
    window.__HY7_TEST_API__ = window.__HY8_TEST_API__;
    init();
  }
})();
